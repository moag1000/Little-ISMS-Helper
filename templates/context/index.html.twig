{% extends 'base.html.twig' %}

{% block title %}ISMS Kontext - Small ISMS Helper{% endblock %}

{% block body %}
<h1>ISMS Kontext</h1>

<p style="color: #666; margin-bottom: 2rem;">
    Definition des Organisationskontexts, Geltungsbereichs und der ISMS-Ziele
</p>

{% if context %}
<div class="info-box">
    <h3>{{ context.organizationName }}</h3>
    {% if context.ismsScope %}
    <p><strong>ISMS Geltungsbereich:</strong></p>
    <p>{{ context.ismsScope }}</p>
    {% endif %}
</div>
{% else %}
<div class="warning-box">
    <p>Noch kein ISMS-Kontext definiert. Bitte erfassen Sie zunächst den Organisationskontext.</p>
</div>
{% endif %}

<h2 style="margin-top: 2rem;">ISMS-Ziele</h2>

{% if objectives|length > 0 %}
<p><strong>Aktive Ziele:</strong> {{ objectives|length }}</p>
{% else %}
<p><em>Noch keine ISMS-Ziele definiert.</em></p>
{% endif %}

<p style="margin-top: 2rem;">
    <em>Erfassungsformulare und detaillierte Verwaltung werden in der nächsten Phase implementiert.</em>
</p>
{% endblock %}
