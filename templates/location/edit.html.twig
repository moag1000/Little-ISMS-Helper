{% extends 'base.html.twig' %}
{% trans_default_domain 'locations' %}

{% block title %}{{ 'location.title.edit'|trans }} - {{ location.name }} - Little ISMS Helper{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.locations'|trans({}, 'nav'), url: path('app_location_index') },
            { label: location.name, url: path('app_location_show', {id: location.id}) },
            { label: 'common.edit'|trans({}, 'messages') }
        ]
    } %}
</div>

<div class="page-header">
    <div>
        <h1>{{ 'location.title.edit'|trans }}: {{ location.name }}</h1>
    </div>
    <div class="button-group">
        <a href="{{ path('app_location_show', {id: location.id}) }}" class="btn btn-secondary">
            ← {{ 'common.back'|trans({}, 'messages') }}
        </a>
    </div>
</div>

<div class="card">
    <div class="card-content">
        {{ form_start(form) }}
            {% include '_components/_auto_form.html.twig' with {
                'form': form,
                'sections': {
                    'basic_info': {
                        'label': 'location.section.basic_info'|trans,
                        'fields': ['name', 'locationType', 'code', 'description']
                    },
                    'address': {
                        'label': 'location.section.address'|trans,
                        'fields': ['address', 'city', 'postalCode', 'country']
                    },
                    'hierarchy': {
                        'label': 'location.section.hierarchy'|trans,
                        'fields': ['parentLocation']
                    },
                    'security': {
                        'label': 'location.section.security'|trans,
                        'fields': ['securityLevel', 'requiresBadgeAccess', 'requiresEscort', 'cameraMonitored', 'accessControlSystem', 'active']
                    },
                    'details': {
                        'label': 'location.section.details'|trans,
                        'fields': ['responsiblePerson', 'capacity', 'squareMeters', 'notes']
                    }
                },
                'col_widths': {
                    'name': 6,
                    'locationType': 6,
                    'code': 6,
                    'description': 6,
                    'address': 12,
                    'city': 4,
                    'postalCode': 4,
                    'country': 4,
                    'parentLocation': 12,
                    'securityLevel': 6,
                    'requiresBadgeAccess': 12,
                    'requiresEscort': 12,
                    'cameraMonitored': 12,
                    'accessControlSystem': 12,
                    'active': 12,
                    'responsiblePerson': 6,
                    'capacity': 3,
                    'squareMeters': 3,
                    'notes': 12
                }
            } %}

            <div class="d-flex justify-content-between mt-4">
                <a href="{{ path('app_location_show', {id: location.id}) }}" class="btn btn-secondary">
                    {{ 'common.cancel'|trans({}, 'messages') }}
                </a>
                <button type="submit" class="btn btn-primary">
                    {{ 'common.save'|trans({}, 'messages') }}
                </button>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
