<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title>{% trans_default_domain 'soa' %}{{ 'soa.page.title'|trans }} - ISO 27001:2022</title>
    <style>
        @page {
            margin: 2cm 1.5cm;
        }

        body {
            font-family: DejaVu Sans, sans-serif;
            font-size: 9pt;
            line-height: 1.4;
            color: #333;
        }

        h1 {
            font-size: 18pt;
            color: #1a56db;
            text-align: center;
            margin-bottom: 5pt;
            border-bottom: 3px solid #1a56db;
            padding-bottom: 10pt;
        }

        h2 {
            font-size: 14pt;
            color: #1a56db;
            margin-top: 15pt;
            margin-bottom: 8pt;
            border-bottom: 1px solid #cbd5e1;
            padding-bottom: 5pt;
        }

        h3 {
            font-size: 11pt;
            color: #334155;
            margin-top: 12pt;
            margin-bottom: 6pt;
            font-weight: bold;
        }

        .header-info {
            text-align: center;
            font-size: 9pt;
            color: #64748b;
            margin-bottom: 20pt;
        }

        .stats-grid {
            display: table;
            width: 100%;
            margin-bottom: 20pt;
            border: 1px solid #cbd5e1;
            border-collapse: collapse;
        }

        .stats-row {
            display: table-row;
        }

        .stats-cell {
            display: table-cell;
            padding: 8pt;
            text-align: center;
            border: 1px solid #cbd5e1;
            width: 25%;
        }

        .stats-label {
            font-size: 8pt;
            color: #64748b;
            text-transform: uppercase;
            font-weight: bold;
        }

        .stats-value {
            font-size: 16pt;
            color: #1a56db;
            font-weight: bold;
            margin-top: 3pt;
        }

        .control-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15pt;
            font-size: 8pt;
        }

        .control-table th {
            background-color: #1a56db;
            color: white;
            padding: 6pt;
            text-align: left;
            font-weight: bold;
            border: 1px solid #1e3a8a;
        }

        .control-table td {
            padding: 5pt;
            border: 1px solid #cbd5e1;
            vertical-align: top;
        }

        .control-table tr:nth-child(even) {
            background-color: #f8fafc;
        }

        .control-id {
            font-weight: bold;
            color: #1a56db;
            white-space: nowrap;
        }

        .badge {
            display: inline-block;
            padding: 2pt 6pt;
            border-radius: 3pt;
            font-size: 7pt;
            font-weight: bold;
            text-transform: uppercase;
        }

        .badge-implemented {
            background-color: #10b981;
            color: white;
        }

        .badge-partial {
            background-color: #f59e0b;
            color: white;
        }

        .badge-not-implemented {
            background-color: #ef4444;
            color: white;
        }

        .badge-planned {
            background-color: #3b82f6;
            color: white;
        }

        .badge-applicable {
            background-color: #10b981;
            color: white;
        }

        .badge-not-applicable {
            background-color: #6b7280;
            color: white;
        }

        .category-header {
            background-color: #e0e7ff;
            padding: 8pt;
            margin-top: 15pt;
            margin-bottom: 8pt;
            border-left: 4px solid #1a56db;
            font-weight: bold;
            font-size: 10pt;
            color: #1e3a8a;
        }

        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: 30pt;
            text-align: center;
            font-size: 7pt;
            color: #64748b;
            border-top: 1px solid #cbd5e1;
            padding-top: 5pt;
        }

        .page-break {
            page-break-before: always;
        }

        .text-muted {
            color: #64748b;
            font-size: 7pt;
        }

        .percentage {
            font-weight: bold;
            color: #059669;
        }

        /* Color variants for stats values */
        .stats-value-success {
            color: #10b981;
        }

        .stats-value-warning {
            color: #f59e0b;
        }

        .stats-value-danger {
            color: #ef4444;
        }

        /* Text alignment */
        .text-center {
            text-align: center;
        }

        /* Column widths */
        .w-8 {
            width: 8%;
        }

        .w-12 {
            width: 12%;
        }

        .w-25 {
            width: 25%;
        }

        .w-35 {
            width: 35%;
        }
    </style>
</head>
<body>
    <h1>{{ 'soa.page.title'|trans }}</h1>
    <div class="header-info">
        {{ 'soa.page.subtitle'|trans }}<br>
        {{ 'soa.report.generated_at'|trans({'%datetime%': generatedAt|date('d.m.Y H:i')}) }}
    </div>

    <!-- Statistics Overview -->
    <h2>{{ 'soa.section.implementation_overview'|trans }}</h2>
    <div class="stats-grid">
        <div class="stats-row">
            <div class="stats-cell">
                <div class="stats-label">{{ 'soa.kpi.total_controls'|trans }}</div>
                <div class="stats-value">{{ stats.total }}</div>
            </div>
            <div class="stats-cell">
                <div class="stats-label">{{ 'soa.kpi.implemented_controls'|trans }}</div>
                <div class="stats-value stats-value-success">{{ stats.implemented }}</div>
            </div>
            <div class="stats-cell">
                <div class="stats-label">{{ 'soa.kpi.in_progress_controls'|trans }}</div>
                <div class="stats-value stats-value-warning">{{ stats.in_progress }}</div>
            </div>
            <div class="stats-cell">
                <div class="stats-label">{{ 'soa.kpi.not_started_controls'|trans }}</div>
                <div class="stats-value stats-value-danger">{{ stats.not_started }}</div>
            </div>
        </div>
    </div>

    <!-- Category Summary -->
    <h2>{{ 'soa.section.controls_by_category'|trans }}</h2>
    <table class="control-table">
        <thead>
            <tr>
                <th scope="col">{{ 'soa.table.category'|trans }}</th>
                <th scope="col">{{ 'soa.table.name'|trans }}</th>
                <th scope="col" class="text-center">{{ 'soa.table.count'|trans }}</th>
            </tr>
        </thead>
        <tbody>
            {% for stat in categoryStats %}
                <tr>
                    <td><strong>{{ ('soa.category.' ~ stat.category)|trans }}</strong></td>
                    <td>{{ ('soa.category.' ~ stat.category)|trans }}</td>
                    <td class="text-center">{{ stat.total }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <!-- Detailed Controls List -->
    <div class="page-break"></div>
    <h2>{{ 'soa.section.detailed_assessment'|trans }}</h2>

    {% for category, controls in controlsByCategory %}
        <div class="category-header">
            {{ ('soa.category.' ~ category)|trans }} ({{ controls|length }})
        </div>

        <table class="control-table">
            <thead>
                <tr>
                    <th scope="col" class="w-8">{{ 'soa.table.control_id'|trans }}</th>
                    <th scope="col" class="w-25">{{ 'soa.table.name'|trans }}</th>
                    <th scope="col" class="w-12">{{ 'soa.table.applicable'|trans }}</th>
                    <th scope="col" class="w-12">{{ 'soa.field.status'|trans }}</th>
                    <th scope="col" class="w-8">{{ 'soa.table.progress'|trans }}</th>
                    <th scope="col" class="w-35">{{ 'soa.table.justification_notes'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for control in controls %}
                    <tr>
                        <td class="control-id">{{ control.controlId }}</td>
                        <td><strong>{{ control.name }}</strong></td>
                        <td>
                            {% if control.applicable %}
                                <span class="badge badge-applicable">{{ 'soa.value.yes'|trans }}</span>
                            {% else %}
                                <span class="badge badge-not-applicable">{{ 'soa.value.no'|trans }}</span>
                            {% endif %}
                        </td>
                        <td>
                            {% if control.implementationStatus == 'implemented' %}
                                <span class="badge badge-implemented">{{ 'soa.status.implemented'|trans }}</span>
                            {% elseif control.implementationStatus == 'partial' %}
                                <span class="badge badge-partial">{{ 'soa.status.partial'|trans }}</span>
                            {% elseif control.implementationStatus == 'planned' %}
                                <span class="badge badge-planned">{{ 'soa.status.planned'|trans }}</span>
                            {% else %}
                                <span class="badge badge-not-implemented">{{ 'soa.status.not_started'|trans }}</span>
                            {% endif %}
                        </td>
                        <td class="text-center">
                            <span class="percentage">{{ control.implementationPercentage }}%</span>
                        </td>
                        <td>
                            {% if not control.applicable and control.justification %}
                                <strong>{{ 'soa.label.not_applicable_prefix'|trans }}</strong> {{ control.justification|slice(0, 200) }}{% if control.justification|length > 200 %}...{% endif %}
                            {% elseif control.implementationNotes %}
                                {{ control.implementationNotes|slice(0, 200) }}{% if control.implementationNotes|length > 200 %}...{% endif %}
                            {% endif %}

                            {% if control.responsiblePerson %}
                                <br><span class="text-muted">{{ 'soa.label.responsible'|trans }} {{ control.responsiblePerson }}</span>
                            {% endif %}

                            {% if control.targetDate %}
                                <br><span class="text-muted">{{ 'soa.table.target_date'|trans }}: {{ control.targetDate|date('d.m.Y') }}</span>
                            {% endif %}

                            {% if control.risks|length > 0 %}
                                <br><span class="text-muted">{{ 'soa.label.linked_risks'|trans }} {{ control.risks|length }}</span>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endfor %}

    <div class="footer">
        {{ 'soa.page.title'|trans }} — {{ 'soa.report.generated_at'|trans({'%datetime%': generatedAt|date('d.m.Y H:i:s')}) }} — {{ 'soa.footer.note'|trans }}
    </div>
</body>
</html>
