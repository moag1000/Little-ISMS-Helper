{% extends 'base.html.twig' %}

{% block title %}{{ 'incident.edit'|trans({}, 'incident') }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row">
        <div class="col-lg-10 col-xl-9 mx-auto">
            <nav aria-label="breadcrumb" class="mb-3">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_incident_index') }}" data-turbo-action="advance">
                            <i class="bi bi-bug" aria-hidden="true"></i> {{ 'nav.incidents'|trans({}, 'nav') }}
                        </a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_incident_show', {id: incident.id}) }}" data-turbo-action="advance">
                            {{ incident.title }}
                        </a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">{{ 'incident.edit'|trans({}, 'incident') }}</li>
                </ol>
            </nav>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3 mb-0">
                    <i class="bi bi-pencil" aria-hidden="true"></i>
                    {{ 'incident.edit'|trans({}, 'incident') }}: {{ incident.title }}
                </h1>
                <a href="{{ path('app_incident_show', {id: incident.id}) }}" class="btn btn-outline-secondary" data-turbo-action="advance">
                    <i class="bi bi-arrow-left" aria-hidden="true"></i> {{ 'common.back'|trans({}, 'messages') }}
                </a>
            </div>

            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">{{ 'incident.edit'|trans({}, 'incident') }}</h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'id': 'incident-form'}}) }}

                    {# Basic Information Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-info-square" aria-hidden="true"></i>
                            {{ 'incident.section.basic_info'|trans({}, 'incident') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-8">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.title,
                                    'label': 'incident.field.title'|trans({}, 'incident'),
                                    'required': true
                                } %}
                            </div>
                            <div class="col-md-4">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.category,
                                    'label': 'incident.field.category'|trans({}, 'incident')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.description,
                                    'label': 'incident.field.description'|trans({}, 'incident')
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Severity & Status Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-thermometer-half" aria-hidden="true"></i>
                            {{ 'incident.section.severity_status'|trans({}, 'incident') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.severity,
                                    'label': 'incident.field.severity'|trans({}, 'incident'),
                                    'required': true
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.status,
                                    'label': 'incident.field.status'|trans({}, 'incident'),
                                    'help': 'incident.status_help'|trans({}, 'incident')
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Detection & Reporting Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-calendar-event" aria-hidden="true"></i>
                            {{ 'incident.section.detection_reporting'|trans({}, 'incident') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-3">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.detectedAt,
                                    'label': 'incident.field.detected_at'|trans({}, 'incident')
                                } %}
                            </div>
                            <div class="col-md-3">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.occurredAt,
                                    'label': 'incident.field.occurred_at'|trans({}, 'incident')
                                } %}
                            </div>
                            <div class="col-md-3">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.reportedBy,
                                    'label': 'incident.field.reported_by'|trans({}, 'incident')
                                } %}
                            </div>
                            {% if form.detectedDate is defined %}
                            <div class="col-md-3">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.detectedDate,
                                    'label': 'incident.field.detected_date'|trans({}, 'incident')
                                } %}
                            </div>
                            {% endif %}
                        </div>
                    </fieldset>

                    {# Affected Systems & Assets Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-hdd-network" aria-hidden="true"></i>
                            {{ 'incident.section.affected_systems'|trans({}, 'incident') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.affectedSystems,
                                    'label': 'incident.field.affected_systems'|trans({}, 'incident')
                                } %}
                            </div>
                            {% if form.affectedAssets is defined %}
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.affectedAssets,
                                    'label': 'incident.field.affected_assets'|trans({}, 'incident')
                                } %}
                            </div>
                            {% endif %}
                        </div>
                    </fieldset>

                    {# Analysis & Resolution Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-search" aria-hidden="true"></i>
                            {{ 'incident.analysis_resolution'|trans({}, 'incident') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.rootCause,
                                    'label': 'incident.field.root_cause'|trans({}, 'incident')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.correctiveActions,
                                    'label': 'incident.field.corrective_actions'|trans({}, 'incident')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.lessonsLearned,
                                    'label': 'incident.field.lessons_learned'|trans({}, 'incident')
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% if form.closedDate is defined %}
                                    {% include '_components/_form_field.html.twig' with {
                                        'field': form.closedDate,
                                        'label': 'incident.field.closed_date'|trans({}, 'incident')
                                    } %}
                                {% elseif form.closedAt is defined %}
                                    {% include '_components/_form_field.html.twig' with {
                                        'field': form.closedAt,
                                        'label': 'incident.field.closed_at'|trans({}, 'incident')
                                    } %}
                                {% endif %}
                            </div>
                        </div>
                    </fieldset>

                    {# Form Actions #}
                    <div class="d-flex justify-content-between align-items-center pt-3 border-top">
                        <a href="{{ path('app_incident_show', {id: incident.id}) }}"
                           class="btn btn-outline-secondary"
                           data-turbo-action="advance">
                            <i class="bi bi-x-circle" aria-hidden="true"></i> {{ 'common.cancel'|trans({}, 'messages') }}
                        </a>
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="bi bi-save" aria-hidden="true"></i> {{ 'incident.update'|trans({}, 'incident') }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
