{% extends 'admin/layout.html.twig' %}

{% block title %}{{ 'user.edit'|trans({}, 'users')|default('Benutzer bearbeiten') }} - {{ user.fullName }}{% endblock %}

{% block admin_content %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>
            <i class="bi bi-person-gear" aria-hidden="true"></i>
            {{ 'user.edit'|trans({}, 'users')|default('Benutzer bearbeiten') }}
        </h1>
        <div>
            <a href="{{ path('user_management_show', {'id': user.id}) }}" class="btn btn-outline-info">
                <i class="bi bi-eye" aria-hidden="true"></i>
                {{ 'common.view'|trans({}, 'messages')|default('Ansehen') }}
            </a>
            <a href="{{ path('user_management_index') }}" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left" aria-hidden="true"></i>
                {{ 'common.back'|trans({}, 'messages') }}
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">{{ user.fullName }} {{ 'user.edit_suffix'|trans({}, 'users')|default('bearbeiten') }}</h3>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        {% include 'user_management/_form.html.twig' with {
                            'button_label': 'user.save_changes'|trans({}, 'users')|default('Änderungen speichern'),
                            'button_icon': 'bi-check-circle',
                            'is_edit_mode': true,
                            'user': user,
                            'back_url': path('user_management_show', {'id': user.id})
                        } %}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock admin_content %}
