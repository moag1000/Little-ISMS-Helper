<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #7c3aed; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .due-date { background: #fef3c7; border: 1px solid #f59e0b; padding: 10px; margin: 15px 0; border-radius: 4px; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📋 {{ 'emails.audit_due.title'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <div class="field">
                <span class="label">{{ 'emails.audit_due.audit'|trans({}, 'emails') }}:</span>
                <span class="value">{{ audit.title }}</span>
            </div>

            <div class="due-date">
                <strong>⏰ {{ 'emails.audit_due.scheduled_date'|trans({}, 'emails') }}:</strong>
                {{ audit.plannedDate|date('d.m.Y') }}
            </div>

            <div class="field">
                <span class="label">{{ 'emails.audit_due.scope'|trans({}, 'emails') }}:</span>
                <span class="value">{{ audit.scope }}</span>
            </div>

            {% if audit.objectives %}
            <div class="field">
                <span class="label">{{ 'emails.audit_due.objectives'|trans({}, 'emails') }}:</span>
                <p class="value">{{ audit.objectives }}</p>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">{{ 'emails.audit_due.status'|trans({}, 'emails') }}:</span>
                <span class="value">{{ audit.status|upper }}</span>
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.common.automated_reminder'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.audit_due.footer_preparations'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
