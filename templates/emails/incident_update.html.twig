<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #2563eb; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .update-box { background: #dbeafe; border-left: 4px solid #2563eb; padding: 15px; margin: 15px 0; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🔄 {{ 'emails.incident.updated'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <div class="field">
                <span class="label">{{ 'emails.incident.title_field'|trans({}, 'emails') }}:</span>
                <span class="value">{{ incident.title }}</span>
            </div>

            <div class="update-box">
                <strong>{{ 'emails.incident.update'|trans({}, 'emails') }}:</strong>
                <p>{{ changeDescription }}</p>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.incident.current_status'|trans({}, 'emails') }}:</span>
                <span class="value">{{ incident.status|upper }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.incident.severity'|trans({}, 'emails') }}:</span>
                <span class="value">{{ incident.severity|upper }}</span>
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.common.automated_notification'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
