<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #059669; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .info-box { background: #d1fae5; border: 1px solid #059669; padding: 15px; margin: 15px 0; border-radius: 4px; color: #065f46; }
        .note { background: #f3f4f6; border-left: 4px solid #10b981; padding: 10px; margin: 15px 0; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>ℹ️ {{ 'email.workflow.workflow_update'|trans({}, 'messages')}}</h1>
        </div>

        <div class="content">
            <p>Dear {{ recipient.firstName }},</p>

            <div class="info-box">
                <strong>📋 You are being notified about a workflow update:</strong>
            </div>

            <div class="field">
                <span class="label">Workflow:</span>
                <span class="value">{{ instance.workflow.name }}</span>
            </div>

            <div class="field">
                <span class="label">Step Completed:</span>
                <span class="value">{{ step.name }}</span>
            </div>

            <div class="field">
                <span class="label">Related Entity:</span>
                <span class="value">{{ instance.entityType }} (ID: {{ instance.entityId }})</span>
            </div>

            <div class="field">
                <span class="label">Step Type:</span>
                <span class="value">Notification (Informational)</span>
            </div>

            <div class="field">
                <span class="label">Workflow Status:</span>
                <span class="value">{{ instance.status|upper }}</span>
            </div>

            {% if instance.initiatedBy %}
            <div class="field">
                <span class="label">Initiated By:</span>
                <span class="value">{{ instance.initiatedBy.firstName }} {{ instance.initiatedBy.lastName }}</span>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">Started:</span>
                <span class="value">{{ instance.startedAt|date('d.m.Y H:i') }}</span>
            </div>

            <div class="note">
                <strong>Note:</strong> This is an informational notification. No action is required from you at this time.
            </div>
        </div>

        <div class="footer">
            <p>{{ 'email.automated_notification'|trans({}, 'messages')}}</p>
            <p>{{ 'email.workflow.login_for_details'|trans({}, 'messages')}}</p>
        </div>
    </div>
</body>
</html>
