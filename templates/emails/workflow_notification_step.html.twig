<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #059669; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .info-box { background: #d1fae5; border: 1px solid #059669; padding: 15px; margin: 15px 0; border-radius: 4px; color: #065f46; }
        .note { background: #f3f4f6; border-left: 4px solid #10b981; padding: 10px; margin: 15px 0; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>ℹ️ {{ 'emails.workflow.workflow_update'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <p>{{ 'emails.common.dear'|trans({}, 'emails') }} {{ recipient.firstName }},</p>

            <div class="info-box">
                <strong>📋 {{ 'emails.workflow.notified_about'|trans({}, 'emails') }}</strong>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.workflow_field'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.workflow.name }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.step_completed'|trans({}, 'emails') }}:</span>
                <span class="value">{{ step.name }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.related_entity'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.entityType }} (ID: {{ instance.entityId }})</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.step_type'|trans({}, 'emails') }}:</span>
                <span class="value">{{ 'emails.workflow.notification_info'|trans({}, 'emails') }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.workflow_status'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.status|upper }}</span>
            </div>

            {% if instance.initiatedBy %}
            <div class="field">
                <span class="label">{{ 'emails.workflow.initiated_by'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.initiatedBy.firstName }} {{ instance.initiatedBy.lastName }}</span>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">{{ 'emails.workflow.started'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.startedAt|date('d.m.Y H:i') }}</span>
            </div>

            <div class="note">
                <strong>{{ 'common.note'|trans({}, 'messages') }}:</strong> {{ 'emails.workflow.note_no_action'|trans({}, 'emails') }}
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.common.automated_notification'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.workflow.login_details'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
