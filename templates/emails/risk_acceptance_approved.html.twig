<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #16a34a; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .success { background: #dcfce7; border: 1px solid #16a34a; padding: 15px; margin: 15px 0; border-radius: 4px; color: #15803d; }
        .info { background: #f3f4f6; border-left: 4px solid #3b82f6; padding: 10px; margin: 15px 0; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>✅ {{ 'emails.risk_acceptance.approved_title'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <p>{{ 'emails.common.dear'|trans({}, 'emails') }} {{ risk_owner.firstName }} {{ risk_owner.lastName }},</p>

            <div class="success">
                <strong>✅ {{ 'emails.risk_acceptance.approved_message'|trans({}, 'emails') }}</strong>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.risk_title'|trans({}, 'emails') }}:</span>
                <span class="value"><strong>{{ risk.title }}</strong></span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.residual_score'|trans({}, 'emails') }}:</span>
                <span class="value">{{ risk.residualRiskLevel }} ({{ risk.residualProbability }} × {{ risk.residualImpact }})</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.approved_by'|trans({}, 'emails') }}:</span>
                <span class="value">{{ approver.fullName }} ({{ approver.email }})</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.approval_date'|trans({}, 'emails') }}:</span>
                <span class="value">{{ approved_at|date('d.m.Y H:i') }}</span>
            </div>

            <div class="info">
                <strong>📋 {{ 'emails.risk_acceptance.next_steps'|trans({}, 'emails') }}:</strong><br>
                • {{ 'emails.risk_acceptance.next_step_1'|trans({}, 'emails') }}<br>
                • {{ 'emails.risk_acceptance.next_step_2'|trans({}, 'emails') }}<br>
                • {{ 'emails.risk_acceptance.next_step_3'|trans({}, 'emails') }}<br>
                • {{ 'emails.risk_acceptance.next_step_4'|trans({}, 'emails') }}
            </div>

            <div class="info">
                <strong>ℹ️ {{ 'emails.risk_acceptance.iso_compliance'|trans({}, 'emails') }}:</strong><br>
                {{ 'emails.risk_acceptance.iso_compliance_text'|trans({}, 'emails') }}
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.common.automated_notification'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.common.footer_compliance'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
