<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #d97706; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .warning-box { background: #fef3c7; border: 1px solid #d97706; padding: 15px; margin: 15px 0; border-radius: 4px; color: #92400e; }
        .deadline { background: #fde68a; border-left: 4px solid #f59e0b; padding: 10px; margin: 15px 0; font-weight: bold; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>⏰ Deadline Warning</h1>
        </div>

        <div class="content">
            <p>Dear {{ recipient.firstName }},</p>

            <div class="warning-box">
                <strong>⚠️ A workflow deadline is approaching!</strong>
            </div>

            <div class="deadline">
                🕐 Only {{ daysRemaining }} day{% if daysRemaining != 1 %}s{% endif %} remaining!
            </div>

            <div class="field">
                <span class="label">Workflow:</span>
                <span class="value">{{ instance.workflow.name }}</span>
            </div>

            <div class="field">
                <span class="label">Related Entity:</span>
                <span class="value">{{ instance.entityType }} (ID: {{ instance.entityId }})</span>
            </div>

            {% if instance.currentStep %}
            <div class="field">
                <span class="label">Current Step:</span>
                <span class="value">{{ instance.currentStep.name }}</span>
            </div>

            <div class="field">
                <span class="label">Step Type:</span>
                <span class="value">{{ instance.currentStep.stepType|upper }}</span>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">Workflow Status:</span>
                <span class="value">{{ instance.status|upper }}</span>
            </div>

            {% if instance.dueDate %}
            <div class="field">
                <span class="label">Due Date:</span>
                <span class="value">{{ instance.dueDate|date('d.m.Y') }}</span>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">Initiated By:</span>
                <span class="value">{{ instance.initiatedBy.firstName }} {{ instance.initiatedBy.lastName }}</span>
            </div>

            <div class="field">
                <span class="label">Started:</span>
                <span class="value">{{ instance.startedAt|date('d.m.Y H:i') }}</span>
            </div>

            <div class="warning-box">
                <strong>Action Recommended:</strong> Please ensure this workflow is completed before the deadline to maintain compliance and avoid delays.
            </div>
        </div>

        <div class="footer">
            <p>This is an automated warning from Little ISMS Helper</p>
            <p>Please log in to the system to review this workflow</p>
        </div>
    </div>
</body>
</html>
