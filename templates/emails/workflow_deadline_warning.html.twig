<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #d97706; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .warning-box { background: #fef3c7; border: 1px solid #d97706; padding: 15px; margin: 15px 0; border-radius: 4px; color: #92400e; }
        .deadline { background: #fde68a; border-left: 4px solid #f59e0b; padding: 10px; margin: 15px 0; font-weight: bold; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>⏰ {{ 'emails.workflow.deadline_warning'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <p>{{ 'emails.common.dear'|trans({}, 'emails') }} {{ recipient.firstName }},</p>

            <div class="warning-box">
                <strong>⚠️ {{ 'emails.workflow.warning_approaching'|trans({}, 'emails') }}</strong>
            </div>

            <div class="deadline">
                🕐 {{ 'emails.workflow.days_remaining'|trans({'%days%': daysRemaining}, 'emails') }}
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.workflow_field'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.workflow.name }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.related_entity'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.entityType }} (ID: {{ instance.entityId }})</span>
            </div>

            {% if instance.currentStep %}
            <div class="field">
                <span class="label">{{ 'emails.workflow.current_step'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.currentStep.name }}</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.workflow.step_type'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.currentStep.stepType|upper }}</span>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">{{ 'emails.workflow.workflow_status'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.status|upper }}</span>
            </div>

            {% if instance.dueDate %}
            <div class="field">
                <span class="label">{{ 'emails.workflow.due_date'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.dueDate|date('d.m.Y') }}</span>
            </div>
            {% endif %}

            {% if instance.initiatedBy %}
            <div class="field">
                <span class="label">{{ 'emails.workflow.initiated_by'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.initiatedBy.firstName }} {{ instance.initiatedBy.lastName }}</span>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">{{ 'emails.workflow.started'|trans({}, 'emails') }}:</span>
                <span class="value">{{ instance.startedAt|date('d.m.Y H:i') }}</span>
            </div>

            <div class="warning-box">
                <strong>{{ 'emails.workflow.action_recommended'|trans({}, 'emails') }}:</strong> {{ 'emails.workflow.action_recommended_text'|trans({}, 'emails') }}
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.workflow.automated_alert'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.workflow.login_review'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
