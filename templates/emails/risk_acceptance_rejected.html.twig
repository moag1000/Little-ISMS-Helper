<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #ea580c; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .warning { background: #fed7aa; border: 1px solid #ea580c; padding: 15px; margin: 15px 0; border-radius: 4px; color: #9a3412; }
        .info { background: #f3f4f6; border-left: 4px solid #dc2626; padding: 10px; margin: 15px 0; }
        .action-required { background: #dbeafe; border: 1px solid #2563eb; padding: 15px; margin: 15px 0; border-radius: 4px; color: #1e40af; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>❌ {{ 'emails.risk_acceptance.rejected_title'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <p>{{ 'emails.common.dear'|trans({}, 'emails') }} {{ risk_owner.firstName }} {{ risk_owner.lastName }},</p>

            <div class="warning">
                <strong>⚠️ {{ 'emails.risk_acceptance.rejected_message'|trans({}, 'emails') }}</strong>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.risk_title'|trans({}, 'emails') }}:</span>
                <span class="value"><strong>{{ risk.title }}</strong></span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.residual_score'|trans({}, 'emails') }}:</span>
                <span class="value">{{ risk.residualRiskLevel }} ({{ risk.residualProbability }} × {{ risk.residualImpact }})</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.rejected_by'|trans({}, 'emails') }}:</span>
                <span class="value">{{ rejector.fullName }} ({{ rejector.email }})</span>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.rejection_date'|trans({}, 'emails') }}:</span>
                <span class="value">{{ rejected_at|date('d.m.Y H:i') }}</span>
            </div>

            <div class="info">
                <strong>📝 {{ 'emails.risk_acceptance.reason'|trans({}, 'emails') }}:</strong><br>
                {{ rejection_reason }}
            </div>

            <div class="action-required">
                <strong>🔧 {{ 'emails.risk_acceptance.required_actions'|trans({}, 'emails') }}:</strong><br>
                {{ 'emails.risk_acceptance.action_intro'|trans({}, 'emails') }}
                <ul style="margin-top: 10px; margin-bottom: 0;">
                    <li>{{ 'emails.risk_acceptance.review_reason'|trans({}, 'emails') }}</li>
                    <li>{{ 'emails.risk_acceptance.identify_controls'|trans({}, 'emails') }}</li>
                    <li>{{ 'emails.risk_acceptance.update_assessment'|trans({}, 'emails') }}</li>
                    <li>{{ 'emails.risk_acceptance.reassess_risk'|trans({}, 'emails') }}</li>
                    <li>{{ 'emails.risk_acceptance.request_again'|trans({}, 'emails') }}</li>
                </ul>
            </div>

            <div class="info">
                <strong>ℹ️ {{ 'emails.risk_acceptance.iso_guidance'|trans({}, 'emails') }}:</strong><br>
                {{ 'emails.risk_acceptance.iso_guidance_text'|trans({}, 'emails') }}
                <ul style="margin-top: 10px; margin-bottom: 0;">
                    <li><strong>{{ 'emails.risk_acceptance.modify'|trans({}, 'emails') }}:</strong> {{ 'emails.risk_acceptance.modify_desc'|trans({}, 'emails') }}</li>
                    <li><strong>{{ 'emails.risk_acceptance.share'|trans({}, 'emails') }}:</strong> {{ 'emails.risk_acceptance.share_desc'|trans({}, 'emails') }}</li>
                    <li><strong>{{ 'emails.risk_acceptance.avoid'|trans({}, 'emails') }}:</strong> {{ 'emails.risk_acceptance.avoid_desc'|trans({}, 'emails') }}</li>
                </ul>
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.common.automated_notification'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.common.footer_compliance'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
