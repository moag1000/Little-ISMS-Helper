<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #dc2626; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .action-required { background: #fee2e2; border: 1px solid #dc2626; padding: 15px; margin: 15px 0; border-radius: 4px; color: #991b1b; }
        .info { background: #f3f4f6; border-left: 4px solid #f59e0b; padding: 10px; margin: 15px 0; }
        .risk-score { background: #fef3c7; border: 2px solid #f59e0b; padding: 15px; margin: 15px 0; border-radius: 4px; text-align: center; }
        .risk-score-high { background: #fee2e2; border: 2px solid #dc2626; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>⚠️ {{ 'emails.risk_acceptance.request_title'|trans({}, 'emails') }}</h1>
        </div>

        <div class="content">
            <p>{{ 'emails.common.dear'|trans({}, 'emails') }} {{ approver.firstName }} {{ approver.lastName }},</p>

            <div class="action-required">
                <strong>✅ {{ 'emails.risk_acceptance.approval_required'|trans({'%level%': approval_level|upper}, 'emails') }}</strong>
            </div>

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.risk_title'|trans({}, 'emails') }}:</span>
                <span class="value"><strong>{{ risk.title }}</strong></span>
            </div>

            {% if risk.description %}
            <div class="field">
                <span class="label">{{ 'emails.incident.description'|trans({}, 'emails') }}:</span>
                <span class="value">{{ risk.description }}</span>
            </div>
            {% endif %}

            <div class="risk-score {{ risk_score >= 8 ? 'risk-score-high' : '' }}">
                <div class="field">
                    <span class="label">{{ 'emails.risk_acceptance.residual_score'|trans({}, 'emails') }}:</span>
                    <span class="value" style="font-size: 24px; font-weight: bold;">{{ risk_score }}</span>
                </div>
                <div style="font-size: 14px; color: #666;">
                    {{ 'risk.field.probability'|trans({}, 'risk') }}: {{ risk.residualProbability }}/5 × {{ 'risk.field.impact'|trans({}, 'risk') }}: {{ risk.residualImpact }}/5
                </div>
            </div>

            <div class="field">
                <span class="label">{{ 'risk.field.treatment_strategy'|trans({}, 'risk') }}:</span>
                <span class="value">{{ risk.treatmentStrategy|upper }}</span>
            </div>

            {% if risk.riskOwner %}
            <div class="field">
                <span class="label">{{ 'risk.field.risk_owner'|trans({}, 'risk') }}:</span>
                <span class="value">{{ risk.riskOwner.fullName }}</span>
            </div>
            {% endif %}

            <div class="field">
                <span class="label">{{ 'emails.risk_acceptance.approval_level'|trans({}, 'emails') }}:</span>
                <span class="value"><strong>{{ approval_level|upper }}</strong></span>
            </div>

            {% if risk_justification %}
            <div class="info">
                <strong>{{ 'emails.risk_acceptance.justification'|trans({}, 'emails') }}:</strong><br>
                {{ risk_justification }}
            </div>
            {% endif %}

            <div class="info">
                <strong>⚠️ {{ 'emails.risk_acceptance.iso_note'|trans({}, 'emails') }}:</strong><br>
                {{ 'emails.risk_acceptance.iso_note_text'|trans({}, 'emails') }}
            </div>

            <div class="info">
                <strong>{{ 'emails.workflow.next_steps'|trans({}, 'emails') }}:</strong><br>
                {{ 'emails.risk_acceptance.next_steps_intro'|trans({}, 'emails') }}
            </div>
        </div>

        <div class="footer">
            <p>{{ 'emails.common.automated_notification'|trans({}, 'emails') }}</p>
            <p>{{ 'emails.common.footer_compliance'|trans({}, 'emails') }}</p>
        </div>
    </div>
</body>
</html>
