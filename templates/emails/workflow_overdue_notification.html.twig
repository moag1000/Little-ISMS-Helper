<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #dc2626; color: white; padding: 20px; text-align: center; }
        .content { background: #f9fafb; padding: 20px; margin: 20px 0; }
        .field { margin: 10px 0; }
        .label { font-weight: bold; color: #666; }
        .value { color: #000; }
        .overdue-alert { background: #fee2e2; border: 1px solid #dc2626; padding: 10px; margin: 15px 0; border-radius: 4px; color: #991b1b; }
        .warning { background: #fef3c7; border-left: 4px solid #f59e0b; padding: 10px; margin: 15px 0; }
        .footer { text-align: center; color: #666; font-size: 12px; padding: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>⚠️ Overdue Workflow Approval</h1>
        </div>

        <div class="content">
            <div class="overdue-alert">
                <strong>🚨 This workflow step requires your immediate attention!</strong>
            </div>

            <div class="field">
                <span class="label">Workflow:</span>
                <span class="value">{{ instance.workflow.name }}</span>
            </div>

            <div class="field">
                <span class="label">Related Entity:</span>
                <span class="value">{{ instance.entityType }} (ID: {{ instance.entityId }})</span>
            </div>

            <div class="field">
                <span class="label">Current Step:</span>
                <span class="value">{{ instance.currentStep.name }}</span>
            </div>

            <div class="field">
                <span class="label">Step Type:</span>
                <span class="value">{{ instance.currentStep.stepType|upper }}</span>
            </div>

            <div class="field">
                <span class="label">Status:</span>
                <span class="value">{{ instance.status|upper }}</span>
            </div>

            <div class="field">
                <span class="label">Started:</span>
                <span class="value">{{ instance.startedAt|date('d.m.Y H:i') }}</span>
            </div>

            {% if instance.currentStep.dueDate %}
            <div class="field">
                <span class="label">Due Date:</span>
                <span class="value">{{ instance.currentStep.dueDate|date('d.m.Y') }}</span>
            </div>
            {% endif %}

            <div class="warning">
                <strong>Action Required:</strong> Please review and complete this workflow step as soon as possible to avoid delays in the approval process.
            </div>
        </div>

        <div class="footer">
            <p>{{ 'email.workflow.automated_alert'|trans({}, 'messages')}}</p>
            <p>{{ 'email.workflow.login_to_process_step'|trans({}, 'messages')}}</p>
        </div>
    </div>
</body>
</html>
