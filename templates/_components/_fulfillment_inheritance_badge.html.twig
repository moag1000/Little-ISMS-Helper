{# Status badge for FulfillmentInheritanceLog.
 # Icons are semantic; text is always visible for screen readers and color-blind users. #}
{% set status = log.reviewStatus %}
{% set variants = {
    'pending_review':    { class: 'bg-warning-subtle text-warning', icon: 'hourglass-split', labelKey: 'compliance_inheritance.status.pending_review' },
    'source_updated':    { class: 'bg-danger',            icon: 'arrow-repeat',    labelKey: 'compliance_inheritance.status.source_updated' },
    'confirmed':         { class: 'bg-info-subtle text-info',  icon: 'link-45deg',      labelKey: 'compliance_inheritance.status.confirmed' },
    'overridden':        { class: 'bg-secondary',              icon: 'pencil-square',   labelKey: 'compliance_inheritance.status.overridden' },
    'rejected':          { class: 'bg-secondary text-body',    icon: 'x-circle',        labelKey: 'compliance_inheritance.status.rejected' },
    'implemented':       { class: 'bg-success',           icon: 'check-circle',    labelKey: 'compliance_inheritance.status.implemented' },
} %}
{% set variant = variants[status]|default(variants.pending_review) %}
<span class="badge {{ variant.class }} d-inline-flex align-items-center gap-1"
      title="{{ variant.labelKey|trans({}, 'compliance_inheritance') }}">
    <i class="bi bi-{{ variant.icon }}" aria-hidden="true"></i>
    <span>{{ variant.labelKey|trans({}, 'compliance_inheritance') }}</span>
</span>
