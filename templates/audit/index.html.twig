{% extends 'base.html.twig' %}

{% block title %}{{ 'audit_management.title'|trans }} - {{ 'app.title'|trans }}{% endblock %}

{% block body %}
<div class="page-header">
    <div>
        <h1><i class="bi bi-clipboard-check" aria-hidden="true"></i> {{ 'audit_management.title'|trans }}</h1>
        <p class="text-muted">{{ 'audit_management.description'|trans }}</p>
    </div>
    <div class="button-group">
        <a href="{{ path('app_audit_new') }}" class="btn btn-primary">
            <i class="bi bi-plus-circle" aria-hidden="true"></i> {{ 'audit.new'|trans }}
        </a>
    </div>
</div>

<div class="stats-grid mb-4">
    <div class="stat-card">
        <div class="stat-icon"><i class="bi bi-calendar-event" aria-hidden="true"></i></div>
        <div class="stat-value">{{ upcoming|length }}</div>
        <div class="stat-label">{{ 'audit_management.planned_audits'|trans }}</div>
    </div>

    <div class="stat-card">
        <div class="stat-icon"><i class="bi bi-collection" aria-hidden="true"></i></div>
        <div class="stat-value">{{ audits|length }}</div>
        <div class="stat-label">{{ 'audit_management.total_audits'|trans }}</div>
    </div>
</div>

{# Filter Section #}
<div class="card mb-4">
    <div class="card-header">
        <h5 class="mb-0"><i class="bi bi-funnel" aria-hidden="true"></i> {{ 'common.filters'|trans }}</h5>
    </div>
    <div class="card-body">
        <form method="get" action="{{ path('app_audit_index') }}" class="row g-3">
            <div class="col-md-3">
                <label for="filter-status" class="form-label">{{ 'audit.field.status'|trans }}</label>
                <select class="form-select" id="filter-status" name="status" onchange="this.form.submit()">
                    <option value="">{{ 'common.all'|trans }}</option>
                    <option value="planned" {{ app.request.get('status') == 'planned' ? 'selected' : '' }}>{{ 'audit.status.planned'|trans }}</option>
                    <option value="in_progress" {{ app.request.get('status') == 'in_progress' ? 'selected' : '' }}>{{ 'audit.status.in_progress'|trans }}</option>
                    <option value="completed" {{ app.request.get('status') == 'completed' ? 'selected' : '' }}>{{ 'audit.status.completed'|trans }}</option>
                    <option value="reported" {{ app.request.get('status') == 'reported' ? 'selected' : '' }}>{{ 'audit.status.reported'|trans }}</option>
                </select>
            </div>

            <div class="col-md-3">
                <label for="filter-scope-type" class="form-label">{{ 'audit.field.scope_type'|trans }}</label>
                <select class="form-select" id="filter-scope-type" name="scope_type" onchange="this.form.submit()">
                    <option value="">{{ 'common.all'|trans }}</option>
                    <option value="full_isms" {{ app.request.get('scope_type') == 'full_isms' ? 'selected' : '' }}>{{ 'audit.scope_type.full_isms'|trans }}</option>
                    <option value="compliance_framework" {{ app.request.get('scope_type') == 'compliance_framework' ? 'selected' : '' }}>{{ 'audit.scope_type.compliance_framework'|trans }}</option>
                    <option value="asset" {{ app.request.get('scope_type') == 'asset' ? 'selected' : '' }}>{{ 'audit.scope_type.asset'|trans }}</option>
                    <option value="asset_type" {{ app.request.get('scope_type') == 'asset_type' ? 'selected' : '' }}>{{ 'audit.scope_type.asset_type'|trans }}</option>
                    <option value="asset_group" {{ app.request.get('scope_type') == 'asset_group' ? 'selected' : '' }}>{{ 'audit.scope_type.asset_group'|trans }}</option>
                    <option value="location" {{ app.request.get('scope_type') == 'location' ? 'selected' : '' }}>{{ 'audit.scope_type.location'|trans }}</option>
                    <option value="department" {{ app.request.get('scope_type') == 'department' ? 'selected' : '' }}>{{ 'audit.scope_type.department'|trans }}</option>
                    <option value="corporate_wide" {{ app.request.get('scope_type') == 'corporate_wide' ? 'selected' : '' }}>{{ 'audit.scope_type.corporate_wide'|trans }}</option>
                    <option value="corporate_subsidiaries" {{ app.request.get('scope_type') == 'corporate_subsidiaries' ? 'selected' : '' }}>{{ 'audit.scope_type.corporate_subsidiaries'|trans }}</option>
                </select>
            </div>

            <div class="col-md-2">
                <label for="filter-date-from" class="form-label">{{ 'common.date_from'|trans }}</label>
                <input type="date" class="form-control" id="filter-date-from" name="date_from" value="{{ app.request.get('date_from') }}" onchange="this.form.submit()">
            </div>

            <div class="col-md-2">
                <label for="filter-date-to" class="form-label">{{ 'common.date_to'|trans }}</label>
                <input type="date" class="form-control" id="filter-date-to" name="date_to" value="{{ app.request.get('date_to') }}" onchange="this.form.submit()">
            </div>

            <div class="col-md-2 d-flex align-items-end">
                <a href="{{ path('app_audit_index') }}" class="btn btn-secondary w-100">
                    <i class="bi bi-x-circle" aria-hidden="true"></i> {{ 'common.reset'|trans }}
                </a>
            </div>
        </form>
    </div>
</div>

{# Audits List #}
<div class="card">
    <div class="card-header">
        <h5 class="mb-0"><i class="bi bi-list-check" aria-hidden="true"></i> {{ 'audit_management.audits_list'|trans }} ({{ audits|length }})</h5>
    </div>
    <div class="card-body">
        {% if audits|length > 0 %}
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th scope="col">{{ 'audit.field.audit_number'|trans }}</th>
                            <th scope="col">{{ 'audit.field.title'|trans }}</th>
                            <th scope="col">{{ 'audit.field.scope_type'|trans }}</th>
                            <th scope="col">{{ 'audit.field.planned_date'|trans }}</th>
                            <th scope="col">{{ 'audit.field.status'|trans }}</th>
                            <th scope="col">{{ 'audit.field.lead_auditor'|trans }}</th>
                            <th scope="col" class="text-center">{{ 'common.actions'|trans }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for audit in audits %}
                        <tr>
                            <td><code>{{ audit.auditNumber }}</code></td>
                            <td>{{ audit.title }}</td>
                            <td>
                                <span class="badge bg-secondary">
                                    {{ ('audit.scope_type.' ~ audit.scopeType)|trans }}
                                </span>
                                {% if audit.auditedSubsidiaries|length > 0 %}
                                <br>
                                <small class="text-muted">
                                    <i class="bi bi-building" aria-hidden="true"></i> {{ audit.auditedSubsidiaries|length }} {{ 'common.subsidiaries'|trans }}
                                </small>
                                {% endif %}
                            </td>
                            <td>{{ audit.plannedDate|date('d.m.Y') }}</td>
                            <td>
                                {% set statusClass = {
                                    'planned': 'info',
                                    'in_progress': 'warning',
                                    'completed': 'success',
                                    'reported': 'primary'
                                } %}
                                <span class="badge bg-{{ statusClass[audit.status] ?? 'secondary' }}">
                                    {{ ('audit.status.' ~ audit.status)|trans }}
                                </span>
                            </td>
                            <td>{{ audit.leadAuditor }}</td>
                            <td class="text-center">
                                <a href="{{ path('app_audit_show', {id: audit.id}) }}" class="btn btn-sm btn-outline-primary" aria-label="{{ 'common.view'|trans }}">
                                    <i class="bi bi-eye" aria-hidden="true"></i>
                                </a>
                                {% if is_granted('ROLE_USER') %}
                                <a href="{{ path('app_audit_edit', {id: audit.id}) }}" class="btn btn-sm btn-outline-secondary" aria-label="{{ 'common.edit'|trans }}">
                                    <i class="bi bi-pencil" aria-hidden="true"></i>
                                </a>
                                {% endif %}
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% else %}
            <div class="alert alert-info">
                <i class="bi bi-info-circle" aria-hidden="true"></i> {{ 'audit_management.no_audits'|trans }}
            </div>
        {% endif %}
    </div>
</div>

<style>
.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: var(--spacing-lg);
}

.stat-card {
    background: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    padding: var(--spacing-lg);
    text-align: center;
    transition: all var(--transition-normal);
}

.stat-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
}

.stat-icon {
    font-size: 2.5rem;
    color: var(--color-secondary);
    margin-bottom: var(--spacing-sm);
}

.stat-value {
    font-size: 2rem;
    font-weight: bold;
    color: var(--color-primary);
}

.stat-label {
    color: var(--color-text-light);
    margin-top: var(--spacing-xs);
}

.mb-4 {
    margin-bottom: var(--spacing-xl);
}
</style>
{% endblock %}
