{#
    Audit Form Partial (Accessible)
    Used by both new.html.twig and edit.html.twig
    WCAG 2.1 AA Compliant
#}

{# ISO 27001 Information Banner #}
<div class="alert alert-info mb-4" role="status">
    <h6 class="alert-heading">
        <i class="bi bi-info-circle-fill" aria-hidden="true"></i>
        {{ 'audit.iso_27001_requirement'|trans({}, 'audit') }}
    </h5>
    <p class="mb-0 small">
        {{ 'audit.iso_27001_clause_info'|trans({}, 'audit') }}
    </p>
</div>

{# Basic Information #}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-info-square" aria-hidden="true"></i>
            {{ 'audit.section.basic_info'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.title,
                    'label': 'audit.field.title'|trans({}, 'audit'),
                    'help': 'audit.help.title'|trans({}, 'audit'),
                    'required': true
                } %}
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.scope,
                    'label': 'audit.field.scope'|trans({}, 'audit'),
                    'help': 'audit.help.scope'|trans({}, 'audit'),
                    'required': true
                } %}
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.scopeType,
                    'label': 'audit.field.scope_type'|trans({}, 'audit'),
                    'help': 'audit.help.scope_type'|trans({}, 'audit')
                } %}
            </div>

            {% if form.scopedFramework is defined %}
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.scopedFramework,
                    'label': 'audit.field.scoped_framework'|trans({}, 'audit'),
                    'help': 'audit.help.scoped_framework'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}

            {% if form.auditType is defined %}
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.auditType,
                    'label': 'audit.field.audit_type'|trans({}, 'audit'),
                    'help': 'audit.help.audit_type'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}
        </div>

        {# Corporate Subsidiaries (conditional) #}
        {% if form.auditedSubsidiaries is defined %}
        <div id="subsidiaries-container" style="display: none;">
            <div class="alert alert-info" role="status">
                <i class="bi bi-info-circle" aria-hidden="true"></i>
                <strong>{{ 'audit.corporate_audit_note_title'|trans({}, 'audit') }}:</strong>
                {{ 'audit.corporate_audit_note'|trans({}, 'audit') }}
            </div>
            {% include '_components/_form_field.html.twig' with {
                'field': form.auditedSubsidiaries,
                'label': 'audit.field.audited_subsidiaries'|trans({}, 'audit'),
                'help': 'audit.help.audited_subsidiaries'|trans({}, 'audit')
            } %}
        </div>
        {% endif %}
    </div>
</fieldset>

{# Framework & Standards (Edit mode only) #}
{% if is_edit_mode|default(false) and form.framework is defined %}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-shield-check" aria-hidden="true"></i>
            {{ 'audit.section.frameworks'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.framework,
                    'label': 'audit.field.framework'|trans({}, 'audit'),
                    'help': 'audit.help.framework'|trans({}, 'audit')
                } %}
            </div>

            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.standard,
                    'label': 'audit.field.standard'|trans({}, 'audit'),
                    'help': 'audit.help.standard'|trans({}, 'audit')
                } %}
            </div>
        </div>
    </div>
</fieldset>
{% endif %}

{# Schedule #}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-calendar-event" aria-hidden="true"></i>
            {{ 'audit.section.schedule'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.plannedDate,
                    'label': 'audit.field.planned_date'|trans({}, 'audit'),
                    'help': 'audit.help.planned_date'|trans({}, 'audit')
                } %}
            </div>

            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.actualDate,
                    'label': 'audit.field.actual_date'|trans({}, 'audit'),
                    'help': 'audit.help.actual_date'|trans({}, 'audit')
                } %}
            </div>
        </div>
    </div>
</fieldset>

{# Team & Responsibilities #}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-people" aria-hidden="true"></i>
            {{ 'audit.section.team'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.leadAuditor,
                    'label': 'audit.field.lead_auditor'|trans({}, 'audit'),
                    'help': 'audit.help.lead_auditor'|trans({}, 'audit')
                } %}
            </div>

            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.auditTeam,
                    'label': 'audit.field.audit_team'|trans({}, 'audit'),
                    'help': 'audit.help.audit_team'|trans({}, 'audit')
                } %}
            </div>

            {% if form.auditee is defined %}
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.auditee,
                    'label': 'audit.field.auditee'|trans({}, 'audit'),
                    'help': 'audit.help.auditee'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}

            {% if form.department is defined %}
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.department,
                    'label': 'audit.field.department'|trans({}, 'audit'),
                    'help': 'audit.help.department'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}
        </div>
    </div>
</fieldset>

{# Status & Results (if in form) #}
{% if form.status is defined %}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-flag" aria-hidden="true"></i>
            {{ 'audit.section.status'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.status,
                    'label': 'audit.field.status'|trans({}, 'audit'),
                    'help': 'audit.help.status'|trans({}, 'audit')
                } %}
            </div>

            {% if form.overallResult is defined %}
            <div class="col-md-6">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.overallResult,
                    'label': 'audit.field.overall_result'|trans({}, 'audit'),
                    'help': 'audit.help.overall_result'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}
        </div>
    </div>
</fieldset>
{% endif %}

{# Objectives & Criteria #}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-bullseye" aria-hidden="true"></i>
            {{ 'audit.section.objectives'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.objectives,
                    'label': 'audit.field.objectives'|trans({}, 'audit'),
                    'help': 'audit.help.objectives'|trans({}, 'audit')
                } %}
            </div>

            {% if form.auditCriteria is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.auditCriteria,
                    'label': 'audit.field.audit_criteria'|trans({}, 'audit'),
                    'help': 'audit.help.audit_criteria'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}

            {% if form.methodology is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.methodology,
                    'label': 'audit.field.methodology'|trans({}, 'audit'),
                    'help': 'audit.help.methodology'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}
        </div>
    </div>
</fieldset>

{# Findings #}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-exclamation-triangle" aria-hidden="true"></i>
            {{ 'audit.section.findings'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.findings,
                    'label': 'audit.field.findings'|trans({}, 'audit'),
                    'help': 'audit.help.findings'|trans({}, 'audit')
                } %}
            </div>

            {% if form.nonconformities is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.nonconformities,
                    'label': 'audit.field.nonconformities'|trans({}, 'audit'),
                    'help': 'audit.help.nonconformities'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}

            {% if form.observations is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.observations,
                    'label': 'audit.field.observations'|trans({}, 'audit'),
                    'help': 'audit.help.observations'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}
        </div>
    </div>
</fieldset>

{# Recommendations & Follow-up #}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-lightbulb" aria-hidden="true"></i>
            {{ 'audit.section.recommendations'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.recommendations,
                    'label': 'audit.field.recommendations'|trans({}, 'audit'),
                    'help': 'audit.help.recommendations'|trans({}, 'audit')
                } %}
            </div>

            {% if form.correctiveActions is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.correctiveActions,
                    'label': 'audit.field.corrective_actions'|trans({}, 'audit'),
                    'help': 'audit.help.corrective_actions'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}

            {% if form.followUpActions is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.followUpActions,
                    'label': 'audit.field.follow_up_actions'|trans({}, 'audit'),
                    'help': 'audit.help.follow_up_actions'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}

            {% if form.conclusion is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.conclusion,
                    'label': 'audit.field.conclusion'|trans({}, 'audit'),
                    'help': 'audit.help.conclusion'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}
        </div>
    </div>
</fieldset>

{# Documentation (Edit mode only) #}
{% if is_edit_mode|default(false) and form.evidenceCollected is defined %}
<fieldset class="card mb-4">
    <div class="card-header bg-light">
        <legend class="h5 mb-0">
            <i class="bi bi-file-earmark-text" aria-hidden="true"></i>
            {{ 'audit.section.documentation'|trans({}, 'audit') }}
        </legend>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.evidenceCollected,
                    'label': 'audit.field.evidence_collected'|trans({}, 'audit'),
                    'help': 'audit.help.evidence_collected'|trans({}, 'audit')
                } %}
            </div>

            {% if form.summary is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.summary,
                    'label': 'audit.field.summary'|trans({}, 'audit'),
                    'help': 'audit.help.summary'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}

            {% if form.notes is defined %}
            <div class="col-12">
                {% include '_components/_form_field.html.twig' with {
                    'field': form.notes,
                    'label': 'audit.field.notes'|trans({}, 'audit'),
                    'help': 'audit.help.notes'|trans({}, 'audit')
                } %}
            </div>
            {% endif %}
        </div>
    </div>
</fieldset>
{% endif %}

{# Form Actions #}
<div class="form-actions d-flex gap-2 {% if is_edit_mode|default(false) %}justify-content-between{% else %}justify-content-end{% endif %} pt-3 border-top">
    {% if is_edit_mode|default(false) %}
    <div>
        <a href="{{ path('app_audit_show', {id: audit.id}) }}"
           class="btn btn-outline-secondary"
           data-turbo-action="advance">
            <i class="bi bi-arrow-left" aria-hidden="true"></i>
            {{ 'common.back'|trans({}, 'messages') }}
        </a>
        <a href="{{ path('app_audit_index') }}"
           class="btn btn-outline-secondary"
           data-turbo-action="advance">
            <i class="bi bi-list" aria-hidden="true"></i>
            {{ 'common.to_list'|trans({}, 'messages') }}
        </a>
    </div>
    {% else %}
    <a href="{{ path('app_audit_index') }}"
       class="btn btn-outline-secondary"
       data-turbo-action="advance">
        <i class="bi bi-arrow-left" aria-hidden="true"></i>
        {{ 'common.back'|trans({}, 'messages') }}
    </a>
    {% endif %}

    <button type="submit" class="btn btn-primary btn-lg">
        <i class="{{ button_icon|default('bi-save') }}" aria-hidden="true"></i>
        {{ button_label|default('common.save'|trans({}, 'messages')) }}
    </button>
</div>

{# JavaScript for conditional subsidiaries field #}
{% if form.auditedSubsidiaries is defined %}
<script>
document.addEventListener('DOMContentLoaded', function() {
    const scopeTypeSelect = document.querySelector('[data-corporate-scope]');
    const subsidiariesContainer = document.getElementById('subsidiaries-container');
    const subsidiariesSelect = document.querySelector('[data-corporate-subsidiaries]');

    if (!scopeTypeSelect || !subsidiariesContainer) {
        return; // Elements not found, exit
    }

    function toggleSubsidiariesField() {
        const selectedScope = scopeTypeSelect.value;
        const isCorporateScope = selectedScope === 'corporate_wide' || selectedScope === 'corporate_subsidiaries';

        if (isCorporateScope) {
            subsidiariesContainer.style.display = 'block';

            // Auto-select all for corporate_wide
            if (selectedScope === 'corporate_wide' && subsidiariesSelect) {
                for (let option of subsidiariesSelect.options) {
                    option.selected = true;
                }
            }
        } else {
            subsidiariesContainer.style.display = 'none';

            // Deselect all when hiding
            if (subsidiariesSelect) {
                for (let option of subsidiariesSelect.options) {
                    option.selected = false;
                }
            }
        }
    }

    // Initial check
    toggleSubsidiariesField();

    // Listen for changes
    scopeTypeSelect.addEventListener('change', toggleSubsidiariesField);
});
</script>
{% endif %}
