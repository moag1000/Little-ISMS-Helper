{% extends 'base.html.twig' %}
{% trans_default_domain 'audits' %}

{% block title %}{{ 'audit.edit'|trans({}, 'audits') }} - {{ audit.title }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row">
        <div class="col-lg-10 col-xl-9 mx-auto">
            <nav aria-label="breadcrumb" class="mb-3">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_audit_index') }}" data-turbo-action="advance">
                            <i class="bi bi-clipboard-check" aria-hidden="true"></i> {{ 'audit_management.title'|trans({}, 'audits') }}
                        </a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_audit_show', {id: audit.id}) }}" data-turbo-action="advance">
                            {{ audit.title }}
                        </a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">{{ 'common.edit'|trans({}, 'messages') }}</li>
                </ol>
            </nav>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3 mb-0">
                    <i class="bi bi-pencil" aria-hidden="true"></i>
                    {{ 'audit.edit'|trans({}, 'audits') }}: {{ audit.title }}
                </h1>
                <div>
                    <a href="{{ path('app_audit_show', {id: audit.id}) }}" class="btn btn-outline-secondary" data-turbo-action="advance">
                        <i class="bi bi-eye" aria-hidden="true"></i> {{ 'common.view'|trans({}, 'messages') }}
                    </a>
                </div>
            </div>

            <div class="alert alert-info mb-4" role="alert">
                <h4 class="alert-heading h6">
                    <i class="bi bi-info-circle-fill" aria-hidden="true"></i> {{ 'audit.iso_27001_requirement'|trans({}, 'audits') }}
                </h5>
                <p class="mb-0 small">
                    {{ 'audit.iso_27001_clause_info'|trans({}, 'audits') }}
                </p>
            </div>

            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">{{ 'audit.edit'|trans({}, 'audits') }}</h3>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'id': 'audit-form'}}) }}

                    {# Basic Information Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-info-square" aria-hidden="true"></i>
                            {{ 'audit.section.basic_info'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.title,
                                    'label': form.title.vars.label|default('audit.field.title'|trans({}, 'audits')),
                                    'required': true
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.scope,
                                    'label': form.scope.vars.label|default('audit.field.scope'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.scopeType,
                                    'label': form.scopeType.vars.label|default('audit.field.scope_type'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.auditType,
                                    'label': form.auditType.vars.label|default('audit.field.audit_type'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12" id="subsidiaries-container" style="display: none;">
                                <div class="alert alert-info">
                                    <i class="bi bi-info-circle" aria-hidden="true"></i>
                                    <strong>Konzernaudit:</strong> Wählen Sie die Tochtergesellschaften aus, die in dieses Audit einbezogen werden sollen.
                                    Bei "Konzernweites Audit" sollten alle relevanten Tochtergesellschaften ausgewählt werden.
                                </div>
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.auditedSubsidiaries,
                                    'label': form.auditedSubsidiaries.vars.label|default('audit.field.audited_subsidiaries'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Framework & Standards Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-shield-check" aria-hidden="true"></i>
                            {{ 'audit.section.frameworks'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.framework,
                                    'label': form.framework.vars.label|default('audit.field.framework'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.standard,
                                    'label': form.standard.vars.label|default('audit.field.standard'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Schedule Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-calendar-event" aria-hidden="true"></i>
                            {{ 'audit.section.schedule'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.plannedDate,
                                    'label': form.plannedDate.vars.label|default('audit.field.planned_date'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.actualDate,
                                    'label': form.actualDate.vars.label|default('audit.field.actual_date'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Team & Responsibilities Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-people" aria-hidden="true"></i>
                            {{ 'audit.section.team'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.leadAuditor,
                                    'label': form.leadAuditor.vars.label|default('audit.field.lead_auditor'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.auditTeam,
                                    'label': form.auditTeam.vars.label|default('audit.field.audit_team'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.auditee,
                                    'label': form.auditee.vars.label|default('audit.field.auditee'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.department,
                                    'label': form.department.vars.label|default('audit.field.department'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Status & Results Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-flag" aria-hidden="true"></i>
                            {{ 'audit.section.status'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.status,
                                    'label': form.status.vars.label|default('audit.field.status'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.overallResult,
                                    'label': form.overallResult.vars.label|default('audit.field.overall_result'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Objectives & Criteria Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-bullseye" aria-hidden="true"></i>
                            {{ 'audit.section.objectives'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.objectives,
                                    'label': form.objectives.vars.label|default('audit.field.objectives'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.auditCriteria,
                                    'label': form.auditCriteria.vars.label|default('audit.field.audit_criteria'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.methodology,
                                    'label': form.methodology.vars.label|default('audit.field.methodology'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Findings Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-exclamation-triangle" aria-hidden="true"></i>
                            {{ 'audit.section.findings'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.findings,
                                    'label': form.findings.vars.label|default('audit.field.findings'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.nonconformities,
                                    'label': form.nonconformities.vars.label|default('audit.field.nonconformities'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.observations,
                                    'label': form.observations.vars.label|default('audit.field.observations'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Recommendations & Follow-up Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-lightbulb" aria-hidden="true"></i>
                            {{ 'audit.section.recommendations'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.recommendations,
                                    'label': form.recommendations.vars.label|default('audit.field.recommendations'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.correctiveActions,
                                    'label': form.correctiveActions.vars.label|default('audit.field.corrective_actions'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.followUpActions,
                                    'label': form.followUpActions.vars.label|default('audit.field.follow_up_actions'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Documentation Section #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-file-earmark-text" aria-hidden="true"></i>
                            {{ 'audit.section.documentation'|trans({}, 'audits') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.evidenceCollected,
                                    'label': form.evidenceCollected.vars.label|default('audit.field.evidence_collected'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.summary,
                                    'label': form.summary.vars.label|default('audit.field.summary'|trans({}, 'audits'))
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.notes,
                                    'label': form.notes.vars.label|default('audit.field.notes'|trans({}, 'audits'))
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Form Actions #}
                    <div class="d-flex justify-content-between align-items-center pt-3 border-top">
                        <div>
                            <a href="{{ path('app_audit_show', {id: audit.id}) }}"
                               class="btn btn-outline-secondary"
                               data-turbo-action="advance">
                                <i class="bi bi-arrow-left" aria-hidden="true"></i> {{ 'common.back'|trans({}, 'messages') }}
                            </a>
                            <a href="{{ path('app_audit_index') }}"
                               class="btn btn-outline-secondary"
                               data-turbo-action="advance">
                                <i class="bi bi-list" aria-hidden="true"></i> {{ 'common.to_list'|trans({}, 'messages') }}
                            </a>
                        </div>
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="bi bi-save" aria-hidden="true"></i> {{ 'common.save_changes'|trans({}, 'messages') }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>

            {# Metadata Card #}
            <div class="card shadow mt-4">
                <div class="card-header bg-light">
                    <h3 class="h6 mb-0"><i class="bi bi-info-circle" aria-hidden="true"></i> {{ 'common.metadata'|trans({}, 'messages') }}</h3>
                </div>
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-3 text-muted">{{ 'common.created_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-9">{{ audit.createdAt|date('d.m.Y H:i') }}</dd>

                        <dt class="col-sm-3 text-muted">{{ 'common.updated_at'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-9">{{ audit.updatedAt|date('d.m.Y H:i') }}</dd>

                        <dt class="col-sm-3 text-muted">{{ 'common.id'|trans({}, 'messages') }}</dt>
                        <dd class="col-sm-9"><code>{{ audit.id }}</code></dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .form-check-input {
        cursor: pointer;
    }
    .form-check-label {
        cursor: pointer;
    }
    select[multiple] {
        min-height: 150px;
    }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const scopeTypeSelect = document.querySelector('[data-corporate-scope]');
    const subsidiariesContainer = document.getElementById('subsidiaries-container');
    const subsidiariesSelect = document.querySelector('[data-corporate-subsidiaries]');

    if (!scopeTypeSelect) {
        return; // Exit if scope type field not found
    }

    function toggleSubsidiariesField() {
        const selectedScope = scopeTypeSelect.value;
        const isCorporateScope = selectedScope === 'corporate_wide' || selectedScope === 'corporate_subsidiaries';

        if (isCorporateScope) {
            subsidiariesContainer.style.display = 'block';

            // Auto-select all for corporate_wide
            if (selectedScope === 'corporate_wide' && subsidiariesSelect) {
                for (let option of subsidiariesSelect.options) {
                    option.selected = true;
                }
            }
        } else {
            subsidiariesContainer.style.display = 'none';
            // Deselect all when hiding
            if (subsidiariesSelect) {
                for (let option of subsidiariesSelect.options) {
                    option.selected = false;
                }
            }
        }
    }

    // Run on page load to show field if already corporate scope
    toggleSubsidiariesField();

    // Run on scope type change
    scopeTypeSelect.addEventListener('change', toggleSubsidiariesField);
});

// Also handle Turbo navigation
document.addEventListener('turbo:load', function() {
    const event = new Event('DOMContentLoaded');
    document.dispatchEvent(event);
});
</script>
{% endblock %}
