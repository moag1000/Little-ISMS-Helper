<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>ISMS Dashboard Report</title>
    <style>
        body { font-family: 'DejaVu Sans', sans-serif; font-size: 11pt; }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 3px solid transparent;
            border-image: linear-gradient(90deg, #06b6d4 0%, #ec4899 50%, #8b5cf6 100%) 1;
            padding-bottom: 10px;
        }
        .header h1 {
            color: #0f172a;
            margin: 0;
            font-size: 24pt;
            text-transform: uppercase;
            letter-spacing: 0.5pt;
        }
        .header .date { color: #666; font-size: 10pt; margin-top: 5px; }
        .stats-grid { display: table; width: 100%; margin: 20px 0; }
        .stat-box {
            display: table-cell;
            width: 33%;
            padding: 15px;
            border: 2px solid #cbd5e1;
            text-align: center;
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        }
        .stat-box .number {
            font-size: 32pt;
            font-weight: bold;
            color: #06b6d4;
            text-shadow: 0 0 1pt rgba(6, 182, 212, 0.3);
        }
        .stat-box .label {
            font-size: 10pt;
            color: #0f172a;
            text-transform: uppercase;
            font-weight: bold;
            letter-spacing: 0.5pt;
        }
        .section { margin: 30px 0; page-break-inside: avoid; }
        .section h2 {
            background: linear-gradient(90deg, #0f172a 0%, #1e293b 100%);
            color: #06b6d4;
            padding: 10px;
            font-size: 14pt;
            margin-top: 0;
            text-transform: uppercase;
            letter-spacing: 0.3pt;
        }
        table { width: 100%; border-collapse: collapse; margin: 10px 0; }
        th {
            background: linear-gradient(90deg, #0f172a 0%, #1e293b 100%);
            color: #06b6d4;
            padding: 8px;
            text-align: left;
            font-size: 10pt;
            text-transform: uppercase;
            letter-spacing: 0.3pt;
        }
        td { padding: 8px; border-bottom: 1px solid #ddd; font-size: 10pt; }
        .footer { position: fixed; bottom: 20px; left: 0; right: 0; text-align: center; font-size: 9pt; color: #666; }
    </style>
</head>
<body>
    <div class="header">
        <h1>ISMS Dashboard Report</h1>
        <div class="date">Generated: {{ generated_at|date('d.m.Y H:i') }}</div>
    </div>

    <div class="stats-grid">
        <div class="stat-box">
            <div class="number">{{ data.assets_count }}</div>
            <div class="label">Assets</div>
        </div>
        <div class="stat-box">
            <div class="number">{{ data.risks_count }}</div>
            <div class="label">Total Risks</div>
        </div>
        <div class="stat-box">
            <div class="number" style="color: #dc2626;">{{ data.high_risks }}</div>
            <div class="label">High/Critical</div>
        </div>
    </div>

    <div class="stats-grid">
        <div class="stat-box">
            <div class="number">{{ data.controls_count }}</div>
            <div class="label">Controls</div>
        </div>
        <div class="stat-box">
            <div class="number" style="color: #16a34a;">{{ data.implemented_controls }}</div>
            <div class="label">Implemented</div>
        </div>
        <div class="stat-box">
            <div class="number">{{ data.compliance_percentage }}%</div>
            <div class="label">Compliance</div>
        </div>
    </div>

    <div class="section">
        <h2>Key Metrics</h2>
        <table>
            <tr>
                <th scope="col">Metric</th>
                <th scope="col">Value</th>
                <th scope="col">Status</th>
            </tr>
            <tr>
                <td>Open Incidents</td>
                <td>{{ data.open_incidents }}</td>
                <td>{% if data.open_incidents > 5 %}⚠ Attention{% else %}✓ Normal{% endif %}</td>
            </tr>
            <tr>
                <td>Total Trainings</td>
                <td>{{ data.total_trainings }}</td>
                <td>✓ Active</td>
            </tr>
            <tr>
                <td>Audits This Year</td>
                <td>{{ data.audits_this_year }}</td>
                <td>{% if data.audits_this_year >= 2 %}✓ Compliant{% else %}⚠ Review{% endif %}</td>
            </tr>
        </table>
    </div>

    <div class="footer">
        <p>Confidential - Little ISMS Helper | Page 1</p>
    </div>
</body>
</html>
