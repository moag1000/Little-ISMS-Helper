{% extends 'base.html.twig' %}
{% trans_default_domain 'risk_appetite' %}

{% block title %}{{ 'risk_appetite.edit'|trans }}: {{ appetite.displayName }}{% endblock %}

{% block body %}
<div class="container">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.risk_appetite'|trans({}, 'nav'), url: path('app_risk_appetite_index') },
            { label: appetite.displayName, url: path('app_risk_appetite_show', {id: appetite.id}) },
            { label: 'common.edit'|trans({}, 'messages') }
        ]
    } %}

    <div class="page-header">
        <h1>{{ 'risk_appetite.edit'|trans }}: {{ appetite.displayName }}</h1>
        <a href="{{ path('app_risk_appetite_show', {id: appetite.id}) }}" class="btn btn-secondary">{{ 'common.back'|trans({}, 'messages') }}</a>
    </div>

    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
                {% include '_components/_auto_form.html.twig' with {
                    'form': form,
                    'sections': {
                        'definition': {
                            'label': 'risk_appetite.section.definition'|trans,
                            'fields': ['category', 'maxAcceptableRisk', 'description']
                        },
                        'approval': {
                            'label': 'risk_appetite.section.approval'|trans,
                            'fields': ['isActive', 'approvedBy', 'approvedAt']
                        }
                    },
                    'col_widths': {
                        'category': 6,
                        'maxAcceptableRisk': 6,
                        'description': 12,
                        'isActive': 4,
                        'approvedBy': 4,
                        'approvedAt': 4
                    },
                    'help_texts': {
                        'category': 'risk_appetite.help.category'|trans,
                        'maxAcceptableRisk': 'risk_appetite.help.max_acceptable_risk'|trans,
                        'description': 'risk_appetite.help.description'|trans,
                        'isActive': 'risk_appetite.help.is_active'|trans,
                        'approvedBy': 'risk_appetite.help.approved_by'|trans,
                        'approvedAt': 'risk_appetite.help.approved_at'|trans
                    }
                } %}

                <div class="alert alert-info mt-4">
                    {{ 'risk_appetite.info.iso27005'|trans }}
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a href="{{ path('app_risk_appetite_show', {id: appetite.id}) }}" class="btn btn-secondary">
                        {{ 'common.cancel'|trans({}, 'messages') }}
                    </a>
                    <button type="submit" class="btn btn-primary">
                        {{ 'common.save'|trans({}, 'messages') }}
                    </button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
