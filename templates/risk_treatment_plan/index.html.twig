{% extends 'base.html.twig' %}
{% trans_default_domain 'risk_treatment_plan' %}

{% block title %}{{ 'risk_treatment_plan.title'|trans({}, 'risk_treatment_plan') }} - {{ 'app.title'|trans({}, 'messages') }}{% endblock %}

{% block body %}
{% import '_macros/cards.html.twig' as cards %}

<div class="container">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.risks'|trans({}, 'nav'), url: path('app_risk_index') },
            { label: 'nav.treatment_plans'|trans({}, 'nav') }
        ]
    } %}
</div>

<div class="page-header">
    <div>
        <h1><i class="bi bi-clipboard-check" aria-hidden="true"></i> {{ 'risk_treatment_plan.title'|trans({}, 'risk_treatment_plan') }}</h1>
        <p class="text-muted">{{ 'risk_treatment_plan.description'|trans({}, 'risk_treatment_plan') }}</p>
    </div>
    <div class="button-group">
        <a href="{{ path('app_risk_treatment_plan_new') }}" class="btn btn-primary">
            <i class="bi bi-plus-circle" aria-hidden="true"></i> {{ 'risk_treatment_plan.new'|trans({}, 'risk_treatment_plan') }}
        </a>
    </div>
</div>

<div class="stats-grid mb-4">
    <div class="col-auto">
        {{ cards.kpi_card('risk_treatment_plan.stats.total'|trans({}, 'risk_treatment_plan'), stats.total, null, null, 'primary', null) }}
    </div>
    <div class="col-auto">
        {{ cards.kpi_card('risk_treatment_plan.stats.planned'|trans({}, 'risk_treatment_plan'), stats.planned, null, null, 'secondary', null) }}
    </div>
    <div class="col-auto">
        {{ cards.kpi_card('risk_treatment_plan.stats.in_progress'|trans({}, 'risk_treatment_plan'), stats.in_progress, null, null, 'info', null) }}
    </div>
    <div class="col-auto">
        {{ cards.kpi_card('risk_treatment_plan.stats.completed'|trans({}, 'risk_treatment_plan'), stats.completed, null, null, 'success', null) }}
    </div>
    <div class="col-auto">
        {{ cards.kpi_card('risk_treatment_plan.stats.overdue'|trans({}, 'risk_treatment_plan'), overduePlans|length, null, null, 'danger', null) }}
    </div>
    <div class="col-auto">
        {{ cards.kpi_card('risk_treatment_plan.stats.avg_completion'|trans({}, 'risk_treatment_plan'), stats.avg_completion|number_format(0) ~ '%', null, stats.avg_completion, 'warning', null) }}
    </div>
</div>

{# Skip link for filters section - WCAG 2.4.1 #}
<a href="#filter-section" class="skip-link">{{ 'accessibility.skip_to_filters'|trans({}, 'messages') }}</a>

{# Filter Section #}
<div class="card mb-4">
    <div class="card-header">
        <h3><i class="bi bi-funnel" aria-hidden="true"></i> {{ 'common.filters'|trans({}, 'messages') }}</h3>
    </div>
    <div class="card-body" id="filter-section" tabindex="-1">
        <form method="get" action="{{ path('app_risk_treatment_plan_index') }}" class="row g-3" role="search" aria-label="{{ 'common.filters'|trans({}, 'messages') }}">
            <div class="col-md-3">
                <label for="filter-status" class="form-label">{{ 'risk_treatment_plan.field.status'|trans({}, 'risk_treatment_plan') }}</label>
                <select class="form-select" id="filter-status" name="status" data-controller="ui-actions" data-action="change->ui-actions#autoSubmit">
                    <option value="">{{ 'common.all'|trans({}, 'messages') }}</option>
                    <option value="planned" {{ app.request.query.get('status') == 'planned' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.planned'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="in_progress" {{ app.request.query.get('status') == 'in_progress' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.in_progress'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="completed" {{ app.request.query.get('status') == 'completed' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.completed'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="on_hold" {{ app.request.query.get('status') == 'on_hold' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.on_hold'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="cancelled" {{ app.request.query.get('status') == 'cancelled' ? 'selected' : '' }}>{{ 'risk_treatment_plan.status.cancelled'|trans({}, 'risk_treatment_plan') }}</option>
                </select>
            </div>
            <div class="col-md-3">
                <label for="filter-priority" class="form-label">{{ 'risk_treatment_plan.field.priority'|trans({}, 'risk_treatment_plan') }}</label>
                <select class="form-select" id="filter-priority" name="priority" data-controller="ui-actions" data-action="change->ui-actions#autoSubmit">
                    <option value="">{{ 'common.all'|trans({}, 'messages') }}</option>
                    <option value="critical" {{ app.request.query.get('priority') == 'critical' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.critical'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="high" {{ app.request.query.get('priority') == 'high' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.high'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="medium" {{ app.request.query.get('priority') == 'medium' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.medium'|trans({}, 'risk_treatment_plan') }}</option>
                    <option value="low" {{ app.request.query.get('priority') == 'low' ? 'selected' : '' }}>{{ 'risk_treatment_plan.priority.low'|trans({}, 'risk_treatment_plan') }}</option>
                </select>
            </div>
            <div class="col-md-3">
                <label for="filter-responsible" class="form-label">{{ 'risk_treatment_plan.field.responsible_person'|trans({}, 'risk_treatment_plan') }}</label>
                <input type="text" class="form-control" id="filter-responsible" name="responsible_person"
                       value="{{ app.request.query.get('responsible_person') }}"
                       placeholder="{{ 'common.search'|trans({}, 'messages') }}...">
            </div>
            <div class="col-md-3">
                <label for="filter-overdue" class="form-label">{{ 'risk_treatment_plan.filter.overdue'|trans({}, 'risk_treatment_plan') }}</label>
                <select class="form-select" id="filter-overdue" name="overdue_only" data-controller="ui-actions" data-action="change->ui-actions#autoSubmit">
                    <option value="">{{ 'common.all'|trans({}, 'messages') }}</option>
                    <option value="1" {{ app.request.query.get('overdue_only') == '1' ? 'selected' : '' }}>{{ 'risk_treatment_plan.filter.overdue_only'|trans({}, 'risk_treatment_plan') }}</option>
                </select>
            </div>
            <div class="col-12">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-search" aria-hidden="true"></i> {{ 'common.filter'|trans({}, 'messages') }}
                </button>
                <a href="{{ path('app_risk_treatment_plan_index') }}" class="btn btn-secondary">
                    <i class="bi bi-x-circle" aria-hidden="true"></i> {{ 'common.reset'|trans({}, 'messages') }}
                </a>
            </div>
        </form>
    </div>
</div>

{# Treatment Plans Table #}
<div class="card">
    <div class="card-header">
        <h3><i class="bi bi-list" aria-hidden="true"></i> {{ 'risk_treatment_plan.list.title'|trans({}, 'risk_treatment_plan') }} ({{ plans|length }})</h3>
    </div>
    {% embed '_components/_table.html.twig' with {
        'noMargin': true
    } %}
        {% block table_head %}
            <tr>
                <th scope="col">{{ 'risk_treatment_plan.field.title'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col">{{ 'risk_treatment_plan.field.risk'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'risk_treatment_plan.field.status'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'risk_treatment_plan.field.priority'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col">{{ 'risk_treatment_plan.field.responsible_person'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'risk_treatment_plan.field.completion'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col">{{ 'risk_treatment_plan.field.target_date'|trans({}, 'risk_treatment_plan') }}</th>
                <th scope="col" class="text-center">{{ 'common.actions'|trans({}, 'messages') }}</th>
            </tr>
        {% endblock %}
        {% block table_body %}
            {% for plan in plans %}
            <tr class="{{ plan.isOverdue() ? 'table-warning' : '' }}">
                <td>
                    <strong>{{ plan.title }}</strong>
                    {% if plan.isOverdue() %}
                        <span class="badge bg-danger ms-2">
                            <i class="bi bi-exclamation-triangle" aria-hidden="true"></i> {{ 'risk_treatment_plan.overdue'|trans({}, 'risk_treatment_plan') }}
                        </span>
                    {% endif %}
                </td>
                <td>
                    {% if plan.risk %}
                        <a href="{{ path('app_risk_show', {id: plan.risk.id}) }}">{{ plan.risk.title }}</a>
                    {% else %}
                        <span class="text-muted">-</span>
                    {% endif %}
                </td>
                <td class="text-center">
                    <span class="badge bg-{{ plan.status == 'completed' ? 'success' : (plan.status == 'in_progress' ? 'primary' : (plan.status == 'cancelled' ? 'secondary' : (plan.status == 'on_hold' ? 'warning' : 'info'))) }}">
                        {{ ('risk_treatment_plan.status.' ~ plan.status)|trans({}, 'risk_treatment_plan')|upper }}
                    </span>
                </td>
                <td class="text-center">
                    <span class="badge bg-{{ plan.priority == 'critical' ? 'danger' : (plan.priority == 'high' ? 'warning' : (plan.priority == 'medium' ? 'info' : 'secondary')) }}">
                        {{ ('risk_treatment_plan.priority.' ~ plan.priority)|trans({}, 'risk_treatment_plan')|upper }}
                    </span>
                </td>
                <td>
                    {% if plan.responsiblePerson %}
                        {{ plan.responsiblePerson.fullName }}
                    {% else %}
                        <span class="text-muted">-</span>
                    {% endif %}
                </td>
                <td class="text-center">
                    <div class="progress" style="height: 20px;">
                        <div class="progress-bar bg-{{ plan.completionPercentage >= 75 ? 'success' : (plan.completionPercentage >= 50 ? 'info' : (plan.completionPercentage >= 25 ? 'warning' : 'danger')) }}"
                             role="progressbar"
                             style="width: {{ plan.completionPercentage }}%"
                             aria-valuenow="{{ plan.completionPercentage }}"
                             aria-valuemin="0"
                             aria-valuemax="100">
                            {{ plan.completionPercentage }}%
                        </div>
                    </div>
                </td>
                <td>
                    {{ plan.targetCompletionDate|date('Y-m-d') }}
                    <br>
                    <small class="text-muted">
                        {% set daysUntil = plan.daysUntilTarget %}
                        {% if daysUntil < 0 %}
                            <span class="text-danger">{{ daysUntil|abs }} {{ 'risk_treatment_plan.days_overdue'|trans({}, 'risk_treatment_plan') }}</span>
                        {% elseif daysUntil == 0 %}
                            <span class="text-warning">{{ 'risk_treatment_plan.due_today'|trans({}, 'risk_treatment_plan') }}</span>
                        {% else %}
                            {{ daysUntil }} {{ 'risk_treatment_plan.days_remaining'|trans({}, 'risk_treatment_plan') }}
                        {% endif %}
                    </small>
                </td>
                <td class="text-center">
                    <div class="btn-group btn-group-sm" role="group" aria-label="{{ 'common.actions'|trans({}, 'messages') }}">
                        <a href="{{ path('app_risk_treatment_plan_show', {id: plan.id}) }}"
                           class="btn btn-outline-primary"
                           title="{{ 'common.view'|trans({}, 'messages') }}" aria-label="{{ 'common.view'|trans({}, 'messages') }}">
                            <i class="bi bi-eye" aria-hidden="true"></i>
                        </a>
                        <a href="{{ path('app_risk_treatment_plan_edit', {id: plan.id}) }}"
                           class="btn btn-outline-secondary"
                           title="{{ 'common.edit'|trans({}, 'messages') }}" aria-label="{{ 'common.edit'|trans({}, 'messages') }}">
                            <i class="bi bi-pencil" aria-hidden="true"></i>
                        </a>
                    </div>
                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="8" class="text-center text-muted">
                    {{ 'risk_treatment_plan.no_plans'|trans({}, 'risk_treatment_plan') }}

                </td>
            </tr>
            {% endfor %}
        {% endblock %}
    {% endembed %}
</div>
{% endblock %}
