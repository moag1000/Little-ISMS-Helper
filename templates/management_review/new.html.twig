{% extends 'base.html.twig' %}
{% trans_default_domain 'management_review' %}

{% block title %}{{ 'management_review.new'|trans({}, 'management_review') }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row">
        <div class="col-lg-10 col-xl-9 mx-auto">
            <nav aria-label="breadcrumb" class="mb-3">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_management_review_index') }}" data-turbo-action="advance">
                            <i class="bi bi-clipboard2-check" aria-hidden="true"></i> {{ 'management_review.title'|trans({}, 'management_review') }}
                        </a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">{{ 'management_review.new'|trans({}, 'management_review') }}</li>
                </ol>
            </nav>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3 mb-0">
                    <i class="bi bi-plus-circle" aria-hidden="true"></i> {{ 'management_review.new'|trans({}, 'management_review') }}
                </h1>
                <a href="{{ path('app_management_review_index') }}" class="btn btn-outline-secondary" data-turbo-action="advance">
                    <i class="bi bi-arrow-left" aria-hidden="true"></i> {{ 'common.back'|trans({}, 'messages') }}
                </a>
            </div>

            <div class="alert alert-info mb-4" role="alert">
                <h4 class="alert-heading h6">
                    <i class="bi bi-info-circle-fill" aria-hidden="true"></i> {{ 'management_review.iso_requirement'|trans({}, 'management_review') }}
                </h5>
                <p class="mb-0 small">
                    {{ 'management_review.iso_clause_93_info'|trans({}, 'management_review') }}
                </p>
            </div>

            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">{{ 'management_review.new'|trans({}, 'management_review') }}</h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

                    {# Basic Information #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-info-square" aria-hidden="true"></i>
                            {{ 'management_review.section.basic_info'|trans({}, 'management_review') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-8">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.title,
                                    'label': 'management_review.field.title'|trans({}, 'management_review'),
                                    'required': true
                                } %}
                            </div>
                            <div class="col-md-4">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.reviewDate,
                                    'label': 'management_review.field.review_date'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.participants,
                                    'label': 'management_review.field.participants'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.status,
                                    'label': 'management_review.field.status'|trans({}, 'management_review')
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Inputs to the Review (Clause 9.3.2) #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-inbox" aria-hidden="true"></i>
                            {{ 'management_review.section.inputs'|trans({}, 'management_review') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.changesRelevantToISMS,
                                    'label': 'management_review.field.changes_relevant_to_isms'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.feedbackFromInterestedParties,
                                    'label': 'management_review.field.feedback_from_interested_parties'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.auditResults,
                                    'label': 'management_review.field.audit_results'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.performanceEvaluation,
                                    'label': 'management_review.field.performance_evaluation'|trans({}, 'management_review')
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Status of Previous Actions #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-arrow-clockwise" aria-hidden="true"></i>
                            {{ 'management_review.section.followup'|trans({}, 'management_review') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.previousReviewActions,
                                    'label': 'management_review.field.previous_review_actions'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.nonConformitiesStatus,
                                    'label': 'management_review.field.non_conformities_status'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.correctiveActionsStatus,
                                    'label': 'management_review.field.corrective_actions_status'|trans({}, 'management_review')
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Outputs from the Review (Clause 9.3.3) #}
                    <fieldset class="mb-4">
                        <legend class="h5 mb-3">
                            <i class="bi bi-send" aria-hidden="true"></i>
                            {{ 'management_review.section.outputs'|trans({}, 'management_review') }}
                        </legend>

                        <div class="row">
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.opportunitiesForImprovement,
                                    'label': 'management_review.field.opportunities_for_improvement'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.resourceNeeds,
                                    'label': 'management_review.field.resource_needs'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.decisions,
                                    'label': 'management_review.field.decisions'|trans({}, 'management_review')
                                } %}
                            </div>
                            <div class="col-md-12">
                                {% include '_components/_form_field.html.twig' with {
                                    'field': form.actionItems,
                                    'label': 'management_review.field.action_items'|trans({}, 'management_review')
                                } %}
                            </div>
                        </div>
                    </fieldset>

                    {# Form Actions #}
                    <div class="d-flex justify-content-between align-items-center pt-3 border-top">
                        <a href="{{ path('app_management_review_index') }}"
                           class="btn btn-outline-secondary"
                           data-turbo-action="advance">
                            <i class="bi bi-arrow-left" aria-hidden="true"></i> {{ 'common.back'|trans({}, 'messages') }}
                        </a>
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="bi bi-save" aria-hidden="true"></i> {{ 'management_review.create'|trans({}, 'management_review') }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
