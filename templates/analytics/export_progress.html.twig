{% extends 'base.html.twig' %}
{% trans_default_domain 'analytics' %}
{% import '_components/_fa_page_header.html.twig' as _fa_page_header %}

{% block title %}{{ 'analytics.export.progress_title'|trans({}, 'analytics') }}{% endblock %}

{% block body %}
<div class="container-fluid">
    {% include '_components/_breadcrumb.html.twig' with {
        breadcrumbs: [
            { label: 'nav.analytics'|trans({}, 'nav'), url: path('app_analytics_dashboard') },
            { label: 'analytics.export.progress_title'|trans({}, 'analytics') }
        ]
    } %}

    {{ _fa_page_header.render({
        badge: { icon: 'nav-download', label: ('analytics.export.badge.' ~ type)|trans({}, 'analytics') },
        title: 'analytics.export.progress_title'|trans({}, 'analytics'),
        subtitle: 'analytics.export.progress_subtitle'|trans({}, 'analytics'),
    }) }}

    {# Turbo cache disabled — live status changes every 3 s #}
    <meta name="turbo-cache-control" content="no-cache">

    <div class="row justify-content-center mt-4">
        <div class="col-lg-8 col-xl-6">
            {% include '_components/_async_job_progress.html.twig' with {
                job_id:    jobId,
                job_name:  'analytics.export.' ~ type,
                job_label: 'analytics.export.progress_title'|trans({}, 'analytics'),
                cancel_url: cancelUrl,
                download_url: downloadUrl,
                download_label: 'analytics.export.download_button'|trans({}, 'analytics'),
            } %}

            <div class="mt-3 text-center text-muted small">
                <i class="fa-icon fa-icon--status-info" aria-hidden="true"></i>
                {{ 'admin.job.worker_hint'|trans({}, 'admin') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
