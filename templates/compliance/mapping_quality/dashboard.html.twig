{% extends 'base.html.twig' %}

{% block title %}Mapping Quality Dashboard{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .chart-container {
            position: relative;
            height: 300px;
        }
    </style>
{% endblock %}

{% block body %}
{% import '_macros/cards.html.twig' as cards %}

<div class="container">
    <div class="page-header">
        <div>
            <h1>🔍 Mapping Quality Dashboard</h1>
            <p class="text-muted">Automatisierte Qualitätsanalyse für Compliance-Mappings</p>
        </div>
        <div class="button-group">
            <a href="{{ path('app_mapping_quality_review_queue') }}" class="btn btn-primary">
                <span class="icon"><i class="bi bi-list-check" aria-hidden="true"></i></span> Review Queue
            </a>
            <a href="{{ path('app_mapping_quality_gaps') }}" class="btn btn-secondary">
                <span class="icon">🔍</span> Gap-Übersicht
            </a>
            <a href="{{ path('app_compliance_index') }}" class="btn btn-secondary">
                <span class="icon">←</span> Zurück zu Compliance
            </a>
        </div>
    </div>

    {# Empty State for No Mappings #}
    {% if quality_stats.total_mappings == 0 %}
    <div class="alert alert-warning">
        <h2><i class="bi bi-exclamation-triangle text-warning" aria-hidden="true"></i> Keine Mappings gefunden</h2>
        <p>{{ 'compliance.mapping_quality.no_mappings_found'|trans({}, 'compliance') }}</p>
        <hr>
        <h6>Nächste Schritte:</h6>
        <ol>
            <li>Prüfen Sie ob Frameworks importiert sind: <code>php bin/console app:list-frameworks</code></li>
            <li>Erstellen Sie Mappings: <code>php bin/console app:create-cross-framework-mappings</code></li>
            <li>Kehren Sie dann hierher zurück</li>
        </ol>
        <a href="{{ path('app_compliance_index') }}" class="btn btn-primary mt-2">← Zurück zu Compliance</a>
    </div>
    {% elseif quality_stats.analyzed_mappings == 0 %}
    <div class="alert alert-info">
        <h4><i class="bi bi-info-circle text-info" aria-hidden="true"></i> Noch keine Analyse durchgeführt</h4>
        <p>{{ quality_stats.total_mappings }} Mappings gefunden, aber noch nicht analysiert.</p>
        <hr>
        <h6>Analyse starten:</h6>
        <ol>
            <li>Sanity Check durchführen: <code>php bin/console app:mapping-quality:sanity-check</code></li>
            <li>Analyse starten: <code>php bin/console app:analyze-mapping-quality</code></li>
            <li>Seite neu laden</li>
        </ol>
    </div>
    {% endif %}

    {# Quality Statistics Cards #}
    {% if quality_stats.total_mappings > 0 %}
    <div class="row mb-4">
        <div class="col-md-3">
            {{ cards.kpi_card('Total Mappings', quality_stats.total_mappings, null, null, 'primary') }}
        </div>
        <div class="col-md-3">
            {{ cards.kpi_card('Analysiert', quality_stats.analyzed_mappings, quality_stats.analysis_coverage ~ '%', null, 'success') }}
        </div>
        <div class="col-md-3">
            {{ cards.kpi_card('Benötigt Review', quality_stats.requires_review, null, null, 'warning') }}
        </div>
        <div class="col-md-3">
            {{ cards.kpi_card('Mit Gaps', quality_stats.with_unresolved_gaps, null, null, 'danger') }}
        </div>
    </div>

    {# Average Scores #}
    <div class="row mb-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="mb-0">⭐ Durchschnittliche Qualität</h3>
                </div>
                <div class="card-body">
                    <div class="progress mb-2" style="height: 30px;">
                        <div class="progress-bar {% if quality_stats.avg_quality_score >= 75 %}bg-success{% elseif quality_stats.avg_quality_score >= 50 %}bg-warning{% else %}bg-danger{% endif %}"
                             role="progressbar"
                             style="width: {{ quality_stats.avg_quality_score }}%"
                             aria-valuenow="{{ quality_stats.avg_quality_score }}"
                             aria-valuemin="0"
                             aria-valuemax="100">
                            <strong>{{ quality_stats.avg_quality_score }}%</strong>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="mb-0"><i class="bi bi-bullseye" aria-hidden="true"></i> Durchschnittliches Confidence</h3>
                </div>
                <div class="card-body">
                    <div class="progress mb-2" style="height: 30px;">
                        <div class="progress-bar {% if quality_stats.avg_confidence >= 80 %}bg-success{% elseif quality_stats.avg_confidence >= 60 %}bg-warning{% else %}bg-danger{% endif %}"
                             role="progressbar"
                             style="width: {{ quality_stats.avg_confidence }}%"
                             aria-valuenow="{{ quality_stats.avg_confidence }}"
                             aria-valuemin="0"
                             aria-valuemax="100">
                            <strong>{{ quality_stats.avg_confidence }}%</strong>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {# Batch Analysis UI - Always visible #}
    <div class="card mb-4" id="batchAnalysisCard">
        <div class="card-header {% if quality_stats.unanalyzed_mappings > 0 %}bg-info{% else %}bg-secondary{% endif %} text-white">
            <h3 class="mb-0">🚀 Mapping-Analyse</h3>
        </div>
        <div class="card-body">
            {% if quality_stats.unanalyzed_mappings > 0 %}
            <p class="mb-3">
                <strong>{{ quality_stats.unanalyzed_mappings }} Mappings</strong> warten auf Analyse.
                Starten Sie die Analyse in kleineren Batches direkt hier in der UI.
            </p>
            {% else %}
            <p class="mb-3">
                <span class="badge bg-success me-2">✓ Alle Mappings analysiert</span>
                Sie können eine Re-Analyse starten um die Qualitätswerte mit dem neuesten Algorithmus neu zu berechnen.
            </p>
            {% endif %}

            <div class="row mb-3">
                <div class="col-md-12">
                    <div class="btn-toolbar" role="toolbar">
                        <div class="btn-group me-2" role="group" aria-label="Quick Analysis">
                            <button type="button" class="btn btn-outline-primary" onclick="startBatchAnalysis(10, false)">
                                <i class="bi bi-play-circle"></i> 10 analysieren
                            </button>
                            <button type="button" class="btn btn-outline-primary" onclick="startBatchAnalysis(25, false)">
                                <i class="bi bi-play-circle"></i> 25 analysieren
                            </button>
                            <button type="button" class="btn btn-outline-primary" onclick="startBatchAnalysis(50, false)">
                                <i class="bi bi-play-circle"></i> 50 analysieren
                            </button>
                        </div>
                        <div class="btn-group me-2" role="group" aria-label="Full Analysis">
                            <button type="button" class="btn btn-success" onclick="startBatchAnalysis('all', false)">
                                <i class="bi bi-lightning-charge"></i> Alle analysieren
                            </button>
                        </div>
                        {% if quality_stats.analyzed_mappings > 0 %}
                        <div class="btn-group" role="group" aria-label="Re-Analysis">
                            <button type="button" class="btn btn-outline-warning" onclick="startBatchAnalysis('all', true)" title="Re-analysiert alle Mappings mit neuestem Algorithmus">
                                <i class="bi bi-arrow-clockwise"></i> Re-Analyse
                            </button>
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>

            {# Progress Section (hidden initially) #}
            <div id="analysisProgress" style="display: none;">
                <hr>
                <h4 class="mb-3">Analyse läuft...</h4>

                <div class="progress mb-3" style="height: 30px;">
                    <div id="progressBar" class="progress-bar progress-bar-striped progress-bar-animated bg-info"
                         role="progressbar" style="width: 0%">
                        <strong id="progressText">0%</strong>
                    </div>
                </div>

                <div class="row text-center mb-3">
                    <div class="col-md-3">
                        <small class="text-muted">Analysiert</small>
                        <h5 id="analyzedCount">0</h5>
                    </div>
                    <div class="col-md-3">
                        <small class="text-muted">Verbleibend</small>
                        <h5 id="remainingCount">{{ quality_stats.unanalyzed_mappings }}</h5>
                    </div>
                    <div class="col-md-3">
                        <small class="text-muted">Fehler</small>
                        <h5 id="errorCount" class="text-danger">0</h5>
                    </div>
                    <div class="col-md-3">
                        <small class="text-muted">Status</small>
                        <h5 id="statusText" class="text-info">Bereit</h5>
                    </div>
                </div>

                <button type="button" class="btn btn-danger btn-sm" id="cancelBtn" onclick="cancelAnalysis()">
                    <i class="bi bi-pause-circle" aria-hidden="true"></i> Abbrechen
                </button>

                <div id="analysisLog" class="mt-3" style="max-height: 200px; overflow-y: auto; font-family: monospace; font-size: 12px; background: #f5f5f5; padding: 10px; border-radius: 4px; display: none;">
                </div>
            </div>

            {# Results Section (shown after completion) #}
            <div id="analysisResults" style="display: none;">
                <hr>
                <div class="alert alert-success">
                    <h5><i class="bi bi-check-circle text-success" aria-hidden="true"></i> Analyse abgeschlossen!</h5>
                    <p id="resultsMessage"></p>
                    <button type="button" class="btn btn-primary mt-2" data-controller="ui-actions" data-action="ui-actions#reload">
                        <i class="bi bi-arrow-clockwise" aria-hidden="true"></i> Seite neu laden
                    </button>
                </div>
            </div>
        </div>
    </div>

    {# Charts Row #}
    <div class="row mb-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="mb-0">📊 Confidence-Verteilung</h3>
                </div>
                <div class="card-body">
                    <div class="chart-container">
                        <canvas id="confidenceChart"></canvas>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="mb-0">🔍 Gap-Statistiken nach Priorität</h3>
                </div>
                <div class="card-body">
                    {% if gap_stats is empty %}
                        <p class="text-muted text-center">Keine Gap-Daten verfügbar</p>
                    {% else %}
                        <div class="chart-container">
                            <canvas id="gapStatsChart"></canvas>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    {# Framework Comparison #}
    {% if framework_comparison is not empty %}
    <div class="card mb-4">
        <div class="card-header">
            <h3 class="mb-0">🌍 Framework-Qualitätsvergleich (Top 10)</h3>
        </div>
        <div class="card-body">
            {% embed '_components/_table.html.twig' with {
                'class': 'table-hover',
                'noMargin': true,
                'responsive': true
            } %}
                {% block table_head %}
                    <tr>
                        <th scope="col">Source Framework</th>
                        <th scope="col">Target Framework</th>
                        <th scope="col" class="text-center">Mappings</th>
                        <th scope="col" class="text-center">Ø Percentage</th>
                        <th scope="col" class="text-center">Ø Quality</th>
                        <th scope="col" class="text-center">Ø Confidence</th>
                        <th scope="col" class="text-center">Review nötig</th>
                    </tr>
                {% endblock %}
                {% block table_body %}
                    {% for comparison in framework_comparison|slice(0, 10) %}
                    <tr>
                        <td><strong>{{ comparison.source_framework }}</strong></td>
                        <td>{{ comparison.target_framework }}</td>
                        <td class="text-center">{{ comparison.mapping_count }}</td>
                        <td class="text-center">
                            <span class="badge {% if comparison.avg_percentage >= 70 %}bg-success{% elseif comparison.avg_percentage >= 50 %}badge-warning{% else %}bg-danger{% endif %}">
                                {{ comparison.avg_percentage|round }}%
                            </span>
                        </td>
                        <td class="text-center">
                            <span class="badge {% if comparison.avg_quality >= 70 %}bg-success{% elseif comparison.avg_quality >= 50 %}badge-warning{% else %}bg-danger{% endif %}">
                                {{ comparison.avg_quality|round }}
                            </span>
                        </td>
                        <td class="text-center">
                            <span class="badge {% if comparison.avg_confidence >= 80 %}bg-success{% elseif comparison.avg_confidence >= 60 %}badge-warning{% else %}bg-danger{% endif %}">
                                {{ comparison.avg_confidence|round }}
                            </span>
                        </td>
                        <td class="text-center">
                            {% if comparison.review_count > 0 %}
                                <span class="badge bg-warning">{{ comparison.review_count }}</span>
                            {% else %}
                                <span class="text-success">✓</span>
                            {% endif %}
                        </td>
                    </tr>
                    {% endfor %}
                {% endblock %}
            {% endembed %}
        </div>
    </div>
    {% endif %}

    {# Help Box #}
    <div class="card mb-4">
        <div class="card-header bg-light">
            <h3 class="mb-0"><i class="bi bi-info-circle text-info" aria-hidden="true"></i> Wie funktioniert die Qualitätsanalyse?</h3>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-4">
                    <h4><i class="bi bi-bullseye" aria-hidden="true"></i> Confidence Score</h4>
                    <p class="small">Misst die Zuverlässigkeit der automatischen Analyse:</p>
                    <ul class="small">
                        <li><strong>High (≥80):</strong> Sehr zuverlässig, kann auto-approved werden</li>
                        <li><strong>Medium (60-79):</strong> Gute Basis, optionale Review</li>
                        <li><strong>Low (<60):</strong> Unsicher, manuelle Review erforderlich</li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h4>⭐ Quality Score</h4>
                    <p class="small">Gesamtqualität des Mappings basierend auf:</p>
                    <ul class="small">
                        <li>Confidence (40%)</li>
                        <li>Mapping Percentage (30%)</li>
                        <li>Verification Status (30%)</li>
                    </ul>
                    <p class="small mb-0"><strong>Ziel:</strong> Alle Mappings ≥75 Quality Score</p>
                </div>
                <div class="col-md-4">
                    <h4>🔬 Similarity Metriken</h4>
                    <p class="small">Automatische Berechnung durch:</p>
                    <ul class="small">
                        <li><strong>Keyword Overlap (40%):</strong> Sicherheitsbegriffe</li>
                        <li><strong>Textual Similarity (35%):</strong> Jaccard & Cosine</li>
                        <li><strong>Structural Similarity (25%):</strong> Kategorie, Priorität</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    {% endif %}

    {# Quick Actions - Show only if mappings exist #}
    {% if quality_stats.total_mappings > 0 %}
    <div class="alert alert-info">
        <h5>🚀 Nächste Schritte</h5>
        <ul class="mb-0">
            <li><a href="{{ path('app_mapping_quality_review_queue') }}">Review Queue ansehen</a> - Prüfen Sie Mappings mit niedrigem Confidence</li>
            <li><a href="{{ path('app_mapping_quality_gaps') }}">Gap-Übersicht</a> - Identifizierte Lücken adressieren</li>
            {% if quality_stats.unanalyzed_mappings > 0 %}
            <li><strong>{{ quality_stats.unanalyzed_mappings }} Mappings noch nicht analysiert</strong> - Führen Sie <code>php bin/console app:analyze-mapping-quality</code> aus</li>
            {% endif %}
        </ul>
    </div>
    {% endif %}
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <script>
        // Confidence Distribution Donut Chart
        const confidenceCtx = document.getElementById('confidenceChart');
        if (confidenceCtx) {
            new Chart(confidenceCtx, {
                type: 'doughnut',
                data: {
                    labels: ['High (≥80)', 'Medium (60-79)', 'Low (<60)'],
                    datasets: [{
                        data: [
                            {{ quality_stats.high_confidence }},
                            {{ quality_stats.medium_confidence }},
                            {{ quality_stats.low_confidence }}
                        ],
                        backgroundColor: [
                            '#28a745',
                            '#ffc107',
                            '#dc3545'
                        ],
                        borderColor: [
                            '#28a745',
                            '#ffc107',
                            '#dc3545'
                        ],
                        borderWidth: 2
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'bottom',
                        },
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    const label = context.label || '';
                                    const value = context.parsed || 0;
                                    const total = context.dataset.data.reduce((a, b) => a + b, 0);
                                    const percentage = total > 0 ? ((value / total) * 100).toFixed(1) : 0;
                                    return label + ': ' + value + ' (' + percentage + '%)';
                                }
                            }
                        }
                    }
                }
            });
        }

        // Gap Stats Bar Chart
        const gapStatsCtx = document.getElementById('gapStatsChart');
        if (gapStatsCtx) {
            {% if gap_stats is not empty %}
            const gapLabels = [
                {% for stat in gap_stats %}
                    '{{ stat.priority|upper }}'{% if not loop.last %},{% endif %}
                {% endfor %}
            ];
            const gapCounts = [
                {% for stat in gap_stats %}
                    {{ stat.count }}{% if not loop.last %},{% endif %}
                {% endfor %}
            ];
            const gapImpacts = [
                {% for stat in gap_stats %}
                    {{ stat.totalImpact|round }}{% if not loop.last %},{% endif %}
                {% endfor %}
            ];
            const gapEfforts = [
                {% for stat in gap_stats %}
                    {{ stat.avgEffort|round(1) }}{% if not loop.last %},{% endif %}
                {% endfor %}
            ];

            const backgroundColors = gapLabels.map(label => {
                if (label === 'CRITICAL') return '#721c24';
                if (label === 'HIGH') return '#dc3545';
                if (label === 'MEDIUM') return '#ffc107';
                return '#6c757d';
            });

            new Chart(gapStatsCtx, {
                type: 'bar',
                data: {
                    labels: gapLabels,
                    datasets: [{
                        label: 'Anzahl Gaps',
                        data: gapCounts,
                        backgroundColor: backgroundColors,
                        borderColor: backgroundColors.map(c => c.replace('0.8', '1')),
                        borderWidth: 2
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            display: false
                        },
                        tooltip: {
                            callbacks: {
                                afterLabel: function(context) {
                                    const index = context.dataIndex;
                                    return [
                                        'Impact: ' + gapImpacts[index] + '%',
                                        'Ø Effort: ' + gapEfforts[index] + 'h'
                                    ];
                                }
                            }
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            ticks: {
                                stepSize: 1
                            }
                        }
                    }
                }
            });
            {% endif %}
        }

        // ========================================
        // Batch Analysis Functions
        // ========================================

        let analysisRunning = false;
        let cancelRequested = false;
        let totalToAnalyze = 0;
        let totalAnalyzed = 0;
        let totalErrors = 0;
        let isReanalysis = false;

        function startBatchAnalysis(batchSize, reanalyze = false) {
            if (analysisRunning) {
                alert('Eine Analyse läuft bereits!');
                return;
            }

            isReanalysis = reanalyze;

            // Hide buttons, show progress
            document.querySelectorAll('.btn-toolbar button').forEach(btn => btn.disabled = true);
            document.getElementById('analysisProgress').style.display = 'block';
            document.getElementById('analysisResults').style.display = 'none';

            analysisRunning = true;
            cancelRequested = false;
            totalAnalyzed = 0;
            totalErrors = 0;

            // Determine chunk size and total
            const chunkSize = batchSize === 'all' ? 25 : Math.min(parseInt(batchSize), 25);
            const totalMappings = reanalyze ? {{ quality_stats.total_mappings }} : {{ quality_stats.unanalyzed_mappings }};
            totalToAnalyze = batchSize === 'all' ? totalMappings : Math.min(parseInt(batchSize), totalMappings);

            updateStatus(reanalyze ? 'Starte Re-Analyse...' : 'Starte Analyse...');
            runBatchAnalysis(0, chunkSize, totalToAnalyze, reanalyze);
        }

        async function runBatchAnalysis(offset, chunkSize, totalTarget, reanalyze) {
            if (cancelRequested) {
                finishAnalysis('Analyse abgebrochen', false);
                return;
            }

            if (totalAnalyzed >= totalTarget) {
                finishAnalysis(`✅ Erfolgreich ${totalAnalyzed} Mappings analysiert!`, true);
                return;
            }

            updateStatus('Analysiere...');

            try {
                const response = await fetch('{{ path('app_mapping_quality_batch_analyze') }}', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        limit: chunkSize,
                        offset: offset,
                        reanalyze: reanalyze || false
                    })
                });

                const data = await response.json();

                if (!data.success) {
                    throw new Error(data.error || 'Unknown error');
                }

                // Update counters
                totalAnalyzed += data.analyzed;
                totalErrors += data.errors;

                document.getElementById('analyzedCount').textContent = totalAnalyzed;
                document.getElementById('remainingCount').textContent = data.remaining;
                document.getElementById('errorCount').textContent = totalErrors;

                // Update progress bar
                const progress = Math.min(100, Math.round((totalAnalyzed / totalTarget) * 100));
                document.getElementById('progressBar').style.width = progress + '%';
                document.getElementById('progressText').textContent = progress + '%';

                // Check if done or should continue
                if (data.remaining === 0 || totalAnalyzed >= totalTarget) {
                    finishAnalysis(`✅ Erfolgreich ${totalAnalyzed} Mappings analysiert! ${totalErrors > 0 ? `(${totalErrors} Fehler)` : ''}`, true);
                } else {
                    // Continue with next batch
                    setTimeout(() => {
                        runBatchAnalysis(offset + chunkSize, chunkSize, totalTarget, reanalyze);
                    }, 500); // Small delay between batches
                }

            } catch (error) {
                updateStatus('Fehler: ' + error.message);
                finishAnalysis(`❌ Fehler bei Analyse: ${error.message}`, false);
            }
        }

        function cancelAnalysis() {
            if (confirm('Möchten Sie die Analyse wirklich abbrechen?')) {
                cancelRequested = true;
                updateStatus('Wird abgebrochen...');
            }
        }

        function updateStatus(status) {
            document.getElementById('statusText').textContent = status;
        }

        function finishAnalysis(message, success) {
            analysisRunning = false;

            document.getElementById('analysisProgress').style.display = 'none';
            document.getElementById('analysisResults').style.display = 'block';
            document.getElementById('resultsMessage').innerHTML = message;

            if (success) {
                // Update the alert class
                document.querySelector('#analysisResults .alert').className = 'alert alert-success';
            } else {
                document.querySelector('#analysisResults .alert').className = 'alert alert-warning';
            }

            // Re-enable buttons
            document.querySelectorAll('.btn-toolbar button').forEach(btn => btn.disabled = false);
        }
    </script>
{% endblock %}
