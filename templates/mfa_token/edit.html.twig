{% extends 'admin/layout.html.twig' %}

{% block admin_title %}{{ 'mfa_token.edit'|trans({}, 'mfa') }}{% endblock %}
{% block admin_subtitle %}
    <p class="text-muted">{{ 'mfa_token.edit.subtitle'|trans({}, 'mfa')|default('MFA-Token bearbeiten') }}</p>
{% endblock %}

{% block admin_content %}
    <div class="card">
        <div class="card-header bg-primary text-white"><h5 class="mb-0">{{ 'mfa_token.edit'|trans({}, 'mfa') }}</h5>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="d-flex justify-content-between pt-3 border-top">
                <a href="{{ path('admin_mfa_show', {id: mfa_token.id}) }}" class="btn btn-outline-secondary">
                    {{ 'common.back'|trans({}, 'messages') }}
                </a>
                <button type="submit" class="btn btn-primary">
                    {{ 'common.save'|trans({}, 'messages') }}
                </button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
