{% extends 'base.html.twig' %}

{% block title %}Schritt 9: Basis-Daten - Deployment Wizard{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            {# Progress Bar #}
            <div class="card mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="mb-0">Setup-Fortschritt</h6>
                        <small class="text-muted">Schritt 9 von 11</small>
                    </div>
                    <div class="progress" style="height: 10px;">
                        <div class="progress-bar" role="progressbar" style="width: 82%;" aria-valuenow="82" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                </div>
            </div>

            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">
                        <i class="fas fa-file-import me-2"></i>
                        Schritt 9: Basis-Daten importieren
                    </h2>
                </div>
                <div class="card-body p-4">
                    <p class="lead mb-4">
                        Die folgenden Basis-Daten sind für den Betrieb erforderlich und werden automatisch importiert.
                    </p>

                    {# Base Data List #}
                    <h5 class="mb-3">
                        <i class="fas fa-list-check me-2"></i>
                        Zu importierende Daten
                    </h5>

                    {% for data in base_data %}
                        <div class="card mb-3 border-primary">
                            <div class="card-body">
                                <div class="d-flex align-items-start">
                                    <div class="flex-shrink-0 me-3">
                                        <i class="fas fa-check-circle text-primary fa-2x"></i>
                                    </div>
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1">{{ data.name }}</h6>
                                        <p class="text-muted mb-2">{{ data.description }}</p>
                                        {% if data.command is defined %}
                                            <small class="text-muted">
                                                <i class="fas fa-terminal me-1"></i>
                                                Command: <code>{{ data.command }}</code>
                                            </small>
                                        {% endif %}
                                        {% if data.required_modules is defined %}
                                            <div class="mt-2">
                                                <small class="text-muted">
                                                    <i class="fas fa-puzzle-piece me-1"></i>
                                                    Module:
                                                    {% for module in data.required_modules %}
                                                        <span class="badge bg-secondary">{{ module }}</span>
                                                    {% endfor %}
                                                </small>
                                            </div>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}

                    <div class="alert alert-info mt-4" role="alert">
                        <h6 class="alert-heading">
                            <i class="fas fa-info-circle me-2"></i>
                            Hinweis
                        </h6>
                        <p class="mb-0">
                            Diese Daten sind essentiell für den Betrieb und basieren auf offiziellen Standards (ISO 27001:2022).
                            Der Import kann einige Minuten dauern.
                        </p>
                    </div>

                    {# Import Action #}
                    <form method="post" action="{{ path('setup_step9_base_data_import') }}">
                        <input type="hidden" name="_token" value="{{ csrf_token('setup_base_data') }}">
                        <div class="d-flex justify-content-between mt-4">
                            <a href="{{ path('setup_step8_compliance_frameworks') }}" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>
                                Zurück
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-download me-2"></i>
                                Basis-Daten importieren
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
