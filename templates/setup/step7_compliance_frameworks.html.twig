{% extends 'base.html.twig' %}

{% block title %}{{ 'setup.compliance.title'|trans }} - Deployment Wizard{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            {# Flash Messages #}
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                        {{ message|raw }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {# Progress Bar #}
            <div class="card mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="mb-0">{{ 'setup.progress.title'|trans }}</h6>
                        <small class="text-muted">{{ 'setup.progress.step'|trans({'%current%': 7, '%total%': 10}) }}</small>
                    </div>
                    <div class="progress" style="height: 10px;">
                        <div class="progress-bar" role="progressbar" style="width: 70%;" aria-valuenow="70" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                </div>
            </div>

            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">
                        <i class="fas fa-balance-scale me-2"></i>
                        {{ 'setup.compliance.header'|trans }}
                    </h2>
                </div>
                <div class="card-body p-4">
                    <p class="lead mb-4">
                        {{ 'setup.compliance.description'|trans }}
                    </p>

                    {# Recommended Frameworks Alert #}
                    {% if recommended_frameworks is not empty %}
                        <div class="alert alert-success">
                            <i class="fas fa-lightbulb me-2"></i>
                            <strong>{{ 'setup.compliance.recommended.title'|trans }}</strong>
                            <p class="mb-2">{{ 'setup.compliance.recommended.description'|trans }}</p>
                            <div class="d-flex flex-wrap gap-2">
                                {% for code in recommended_frameworks %}
                                    {% for framework in available_frameworks if framework.code == code %}
                                        <span class="badge bg-success">
                                            {{ framework.icon }} {{ framework.name }}
                                        </span>
                                    {% endfor %}
                                {% endfor %}
                            </div>
                        </div>
                    {% endif %}

                    {# ISO 27001 Mandatory Notice #}
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-2"></i>
                        <strong>{{ 'setup.compliance.mandatory.title'|trans }}</strong>
                        <p class="mb-0">{{ 'setup.compliance.mandatory.message'|trans }}</p>
                    </div>

                    {# Critical Infrastructure Notice for NIS2 #}
                    {% if 'NIS2' in recommended_frameworks and (app.session.get('setup_organisation_employee_count', '1-10') in ['1-10', '11-50']) %}
                        <div class="alert alert-warning">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            <strong>{{ 'setup.compliance.critical_infrastructure.title'|trans }}</strong>
                            <p class="mb-0">{{ 'setup.compliance.critical_infrastructure.message'|trans }}</p>
                        </div>
                    {% endif %}

                    {# Compliance Framework Selection Form #}
                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

                    <h5 class="mb-3">{{ 'setup.compliance.select_frameworks'|trans }}</h5>

                    <div class="row">
                        {% for child in form.frameworks %}
                            <div class="col-md-6 mb-3">
                                <div class="card h-100 {% if loop.index0 in recommended_frameworks or 'ISO27001' in child.vars.value %}border-success{% else %}border-secondary{% endif %}">
                                    <div class="card-body">
                                        <div class="form-check">
                                            {{ form_widget(child, {'attr': {'class': 'form-check-input'}}) }}
                                            {{ form_label(child, null, {'label_attr': {'class': 'form-check-label fw-bold'}}) }}
                                        </div>

                                        {# Find matching framework for description #}
                                        {% set framework_code = child.vars.value %}
                                        {% for framework in available_frameworks if framework.code == framework_code %}
                                            <small class="text-muted d-block mt-2">
                                                {{ framework.description }}
                                            </small>

                                            {# Show recommended badge #}
                                            {% if framework_code in recommended_frameworks %}
                                                <span class="badge bg-success mt-2">
                                                    <i class="fas fa-star me-1"></i>
                                                    {{ 'setup.compliance.recommended_badge'|trans }}
                                                </span>
                                            {% endif %}

                                            {# Show mandatory badge for ISO 27001 #}
                                            {% if framework_code == 'ISO27001' %}
                                                <span class="badge bg-primary mt-2">
                                                    <i class="fas fa-lock me-1"></i>
                                                    {{ 'setup.compliance.mandatory_badge'|trans }}
                                                </span>
                                            {% endif %}
                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>

                    {# Information Box #}
                    <div class="alert alert-warning mt-4">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <strong>{{ 'setup.compliance.note.title'|trans }}</strong>
                        <p class="mb-0">{{ 'setup.compliance.note.message'|trans }}</p>
                    </div>

                    {# Navigation #}
                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('setup_step6_modules') }}" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2"></i>
                            {{ 'setup.navigation.back'|trans }}
                        </a>

                        <button type="submit" class="btn btn-primary">
                            {{ 'setup.navigation.next'|trans }}
                            <i class="fas fa-arrow-right ms-2"></i>
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>

            {# Framework Details Card #}
            <div class="card mt-4 border-info">
                <div class="card-body">
                    <h5 class="card-title">
                        <i class="fas fa-book text-info me-2"></i>
                        {{ 'setup.compliance.details.title'|trans }}
                    </h5>
                    <div class="row">
                        {% for framework in available_frameworks %}
                            <div class="col-md-6 mb-3">
                                <h6>{{ framework.icon }} {{ framework.name }}</h6>
                                <p class="text-muted small mb-0">{{ framework.description }}</p>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
