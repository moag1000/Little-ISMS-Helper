{% extends 'base.html.twig' %}

{% block title %}Schritt 3: Datenbank - Deployment Wizard{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            {# Progress Bar #}
            <div class="card mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="mb-0">Setup-Fortschritt</h6>
                        <small class="text-muted">Schritt 3 von 6</small>
                    </div>
                    <div class="progress" style="height: 10px;">
                        <div class="progress-bar" role="progressbar" style="width: 50%;" aria-valuenow="50" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                </div>
            </div>

            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">
                        <i class="fas fa-database me-2"></i>
                        Schritt 3: Datenbank initialisieren
                    </h2>
                </div>
                <div class="card-body p-4">
                    <p class="lead mb-4">
                        Die Datenbank-Tabellen werden jetzt für die ausgewählten Module erstellt.
                    </p>

                    {# Database Status #}
                    <div class="card mb-4 border-{{ db_status.initialized ? 'success' : 'warning' }}">
                        <div class="card-body">
                            <h5 class="mb-3">
                                <i class="fas fa-{{ db_status.initialized ? 'check-circle text-success' : 'exclamation-triangle text-warning' }} me-2"></i>
                                Datenbank-Status
                            </h5>
                            {% if db_status.initialized %}
                                <p class="text-success mb-2">
                                    <i class="fas fa-check me-2"></i>
                                    Datenbank ist bereits initialisiert
                                </p>
                                <p class="text-muted mb-0">
                                    {{ db_status.total_tables }} Tabellen gefunden
                                </p>
                            {% else %}
                                <p class="text-warning mb-2">
                                    <i class="fas fa-exclamation-triangle me-2"></i>
                                    Datenbank muss initialisiert werden
                                </p>
                                {% if db_status.error is defined %}
                                    <div class="alert alert-danger mb-0">
                                        {{ db_status.error }}
                                    </div>
                                {% else %}
                                    <p class="text-muted mb-0">
                                        Fehlende Tabellen: {{ db_status.missing_tables|length }}
                                    </p>
                                {% endif %}
                            {% endif %}
                        </div>
                    </div>

                    {# Module Overview #}
                    <div class="card bg-light mb-4">
                        <div class="card-body">
                            <h6 class="mb-3">
                                <i class="fas fa-puzzle-piece me-2"></i>
                                Ausgewählte Module ({{ selected_modules|length }})
                            </h6>
                            <div class="row g-2">
                                {% for moduleKey in selected_modules %}
                                    <div class="col-md-4">
                                        <span class="badge bg-primary">{{ moduleKey }}</span>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>

                    {# Migration Info #}
                    <div class="alert alert-info" role="alert">
                        <h6 class="alert-heading">
                            <i class="fas fa-info-circle me-2"></i>
                            Was wird ausgeführt?
                        </h6>
                        <ul class="mb-0">
                            <li>Doctrine Migrations werden ausgeführt</li>
                            <li>Datenbank-Tabellen werden erstellt</li>
                            <li>Indizes und Fremdschlüssel werden angelegt</li>
                            <li>Ihre Daten bleiben erhalten (nur Schema-Änderungen)</li>
                        </ul>
                    </div>

                    {# Migration Action #}
                    <form method="post" action="{{ path('setup_step3_database_migrate') }}">
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('setup_step2_modules') }}" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>
                                Zurück
                            </a>

                            {% if db_status.initialized %}
                                <div>
                                    <button type="submit" class="btn btn-outline-primary me-2">
                                        <i class="fas fa-sync me-2"></i>
                                        Migrationen erneut ausführen
                                    </button>
                                    <a href="{{ path('setup_step4_base_data') }}" class="btn btn-primary">
                                        Weiter zu Schritt 4: Basis-Daten
                                        <i class="fas fa-arrow-right ms-2"></i>
                                    </a>
                                </div>
                            {% else %}
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-play me-2"></i>
                                    Datenbank initialisieren
                                </button>
                            {% endif %}
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
