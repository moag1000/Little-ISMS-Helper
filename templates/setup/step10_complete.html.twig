{% extends 'base.html.twig' %}

{% block title %}Setup abgeschlossen - Deployment Wizard{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            {# Progress Bar #}
            <div class="card mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="mb-0">Setup-Fortschritt</h6>
                        <small class="text-muted">Schritt 10 von 10</small>
                    </div>
                    <div class="progress" style="height: 10px;">
                        <div class="progress-bar bg-success" role="progressbar" style="width: 100%;" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                </div>
            </div>

            <div class="card shadow-lg border-success">
                <div class="card-header bg-success text-white">
                    <h2 class="mb-0">
                        <i class="fas fa-check-circle me-2"></i>
                        Setup erfolgreich abgeschlossen!
                    </h2>
                </div>
                <div class="card-body p-5 text-center">
                    <div class="mb-4">
                        <i class="fas fa-check-circle text-success" style="font-size: 5rem;"></i>
                    </div>

                    <h3 class="mb-4">Ihr ISMS-System ist einsatzbereit!</h3>

                    <p class="lead text-muted mb-4">
                        Alle erforderlichen Komponenten wurden erfolgreich eingerichtet.
                    </p>

                    {# Statistics #}
                    <div class="row g-3 mb-5">
                        <div class="col-md-3">
                            <div class="card bg-light">
                                <div class="card-body">
                                    <h2 class="mb-0 text-primary">{{ statistics.active_modules }}</h2>
                                    <small class="text-muted">Aktive Module</small>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-light">
                                <div class="card-body">
                                    <h2 class="mb-0 text-success">{{ statistics.total_modules }}</h2>
                                    <small class="text-muted">Verfügbare Module</small>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-light">
                                <div class="card-body">
                                    <h2 class="mb-0 text-info">{{ statistics.required_modules }}</h2>
                                    <small class="text-muted">Kern-Module</small>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-light">
                                <div class="card-body">
                                    <h2 class="mb-0 text-warning">{{ statistics.optional_modules }}</h2>
                                    <small class="text-muted">Optionale Module</small>
                                </div>
                            </div>
                        </div>
                    </div>

                    {# Active Modules #}
                    <div class="card bg-light mb-4 text-start">
                        <div class="card-body">
                            <h5 class="mb-3">
                                <i class="fas fa-puzzle-piece me-2"></i>
                                Aktivierte Module
                            </h5>
                            <div class="row g-2">
                                {% for moduleKey in selected_modules %}
                                    <div class="col-md-4">
                                        <span class="badge bg-primary">{{ moduleKey }}</span>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>

                    {# Next Steps #}
                    <div class="card bg-info bg-opacity-10 border-info mb-4 text-start">
                        <div class="card-body">
                            <h5 class="mb-3">
                                <i class="fas fa-lightbulb me-2"></i>
                                Nächste Schritte
                            </h5>
                            <ol class="mb-0">
                                <li class="mb-2">Erkunden Sie das Dashboard und die aktivierten Module</li>
                                <li class="mb-2">Definieren Sie Ihren ISMS-Kontext und Anwendungsbereich</li>
                                <li class="mb-2">Erfassen Sie Ihre Assets und führen Sie eine Risikoanalyse durch</li>
                                <li class="mb-2">Nutzen Sie die Compliance-Module für ISO 27001, TISAX oder DORA</li>
                                <li class="mb-2">Bei Bedarf: Weitere Module über die Modulverwaltung hinzufügen</li>
                            </ol>
                        </div>
                    </div>

                    {# Actions #}
                    <div class="d-grid gap-3">
                        <a href="{{ path('home_index') }}" class="btn btn-lg btn-success">
                            <i class="fas fa-home me-2"></i>
                            Zum Dashboard
                        </a>
                        <div class="row g-2">
                            <div class="col-md-6">
                                <a href="{{ path('module_management_index') }}" class="btn btn-outline-primary w-100">
                                    <i class="fas fa-puzzle-piece me-2"></i>
                                    Module verwalten
                                </a>
                            </div>
                            <div class="col-md-6">
                                <a href="{{ path('context_index') }}" class="btn btn-outline-primary w-100">
                                    <i class="fas fa-cog me-2"></i>
                                    ISMS-Kontext konfigurieren
                                </a>
                            </div>
                        </div>
                    </div>

                    {# Support Info #}
                    <div class="alert alert-light mt-4 text-start" role="alert">
                        <h6 class="alert-heading">
                            <i class="fas fa-question-circle me-2"></i>
                            Hilfe & Dokumentation
                        </h6>
                        <p class="mb-0 small">
                            Bei Fragen oder Problemen konsultieren Sie bitte die Dokumentation oder
                            kontaktieren Sie den Support. Alle Funktionen können über das Hauptmenü erreicht werden.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
