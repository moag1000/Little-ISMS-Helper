{% extends 'base.html.twig' %}

{% block title %}Schritt 9: Beispiel-Daten - Deployment Wizard{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            {# Progress Bar #}
            <div class="card mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="mb-0">Setup-Fortschritt</h6>
                        <small class="text-muted">Schritt 9 von 10</small>
                    </div>
                    <div class="progress" style="height: 10px;">
                        <div class="progress-bar" role="progressbar" style="width: 90%;" aria-valuenow="90" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                </div>
            </div>

            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">
                        <i class="fas fa-file-code me-2"></i>
                        Schritt 9: Beispiel-Daten (Optional)
                    </h2>
                </div>
                <div class="card-body p-4">
                    <p class="lead mb-4">
                        Möchten Sie Beispiel-Daten importieren? Diese helfen Ihnen, das System kennenzulernen.
                    </p>

                    <div class="alert alert-warning" role="alert">
                        <h6 class="alert-heading">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            Hinweis zu Beispiel-Daten
                        </h6>
                        <ul class="mb-0">
                            <li>Beispiel-Daten sind optional und dienen nur zu Demonstrationszwecken</li>
                            <li>Sie können später in einem Produktivsystem gelöscht werden</li>
                            <li>Wählen Sie nur die Beispiel-Daten, die Sie testen möchten</li>
                        </ul>
                    </div>

                    {# Backup Restore Option #}
                    <div class="card border-info mb-4">
                        <div class="card-header bg-info text-white">
                            <h5 class="mb-0">
                                <i class="fas fa-cloud-upload-alt me-2"></i>
                                Backup wiederherstellen (Empfohlen für Migration)
                            </h5>
                        </div>
                        <div class="card-body">
                            <p class="text-muted">
                                Haben Sie ein Backup von einer anderen Installation? Sie können es hier hochladen und wiederherstellen.
                            </p>
                            <form method="post" action="{{ path('setup_step9_restore_backup') }}" enctype="multipart/form-data" id="backupRestoreForm">
                                <input type="hidden" name="_token" value="{{ csrf_token('setup_restore_backup') }}">
                                <div class="mb-3">
                                    <label for="backupFile" class="form-label">Backup-Datei auswählen</label>
                                    <input type="file" class="form-control" id="backupFile" name="backup_file" accept=".json,.gz">
                                    <div class="form-text">Erlaubte Formate: .json, .json.gz</div>
                                </div>
                                <div class="form-check mb-3">
                                    <input class="form-check-input" type="checkbox" id="clearBeforeRestoreSetup" name="clear_before_restore" checked>
                                    <label class="form-check-label" for="clearBeforeRestoreSetup">
                                        Alle bestehenden Daten löschen vor Wiederherstellung (empfohlen)
                                    </label>
                                </div>
                                <div class="mb-3">
                                    <label for="adminPasswordSetup" class="form-label">
                                        <strong>Admin-Passwort setzen</strong>
                                    </label>
                                    <input type="password" class="form-control" id="adminPasswordSetup" name="admin_password" placeholder="Neues Passwort für Admin-Benutzer" required>
                                    <div class="form-text">
                                        <i class="fas fa-info-circle me-1"></i>
                                        Passwörter werden aus Sicherheitsgründen nicht im Backup gespeichert.
                                        Geben Sie hier das Passwort für den Admin-Benutzer an.
                                    </div>
                                </div>
                                <button type="submit" class="btn btn-info">
                                    <i class="fas fa-upload me-2"></i>
                                    Backup hochladen und wiederherstellen
                                </button>
                            </form>

                            {% if backup_restore_result is defined and backup_restore_result %}
                                <div class="mt-3">
                                    {% if backup_restore_result.success %}
                                        <div class="alert alert-success">
                                            <i class="fas fa-check-circle me-2"></i>
                                            <strong>Backup erfolgreich wiederhergestellt!</strong>
                                            <ul class="mb-0 mt-2">
                                                {% for entity, stats in backup_restore_result.statistics %}
                                                    <li>{{ entity }}: {{ stats.created }} erstellt, {{ stats.updated }} aktualisiert</li>
                                                {% endfor %}
                                            </ul>
                                        </div>

                                        {# Orphaned Entities Detection and Repair #}
                                        {% if backup_restore_result.orphaned_entities is defined and backup_restore_result.orphaned_entities.total > 0 %}
                                            <div class="alert alert-warning mt-3">
                                                <h6 class="alert-heading">
                                                    <i class="fas fa-exclamation-triangle me-2"></i>
                                                    Verwaiste Entitäten gefunden
                                                </h6>
                                                <p>
                                                    Nach der Wiederherstellung wurden <strong>{{ backup_restore_result.orphaned_entities.total }}</strong> Entitäten ohne Mandanten-Zuordnung gefunden:
                                                </p>
                                                <ul class="mb-3">
                                                    {% if backup_restore_result.orphaned_entities.assets > 0 %}
                                                        <li><strong>{{ backup_restore_result.orphaned_entities.assets }}</strong> Assets</li>
                                                    {% endif %}
                                                    {% if backup_restore_result.orphaned_entities.risks > 0 %}
                                                        <li><strong>{{ backup_restore_result.orphaned_entities.risks }}</strong> Risiken</li>
                                                    {% endif %}
                                                    {% if backup_restore_result.orphaned_entities.incidents > 0 %}
                                                        <li><strong>{{ backup_restore_result.orphaned_entities.incidents }}</strong> Vorfälle</li>
                                                    {% endif %}
                                                </ul>

                                                <div class="card border-warning">
                                                    <div class="card-body">
                                                        <h6 class="card-title">
                                                            <i class="fas fa-tools me-2"></i>
                                                            Verwaiste Entitäten reparieren
                                                        </h6>
                                                        <p class="card-text text-muted">
                                                            Wählen Sie einen Mandanten, dem die verwaisten Entitäten zugewiesen werden sollen:
                                                        </p>
                                                        <form method="post" action="{{ path('setup_step9_repair_orphans') }}">
                                                            <input type="hidden" name="_token" value="{{ csrf_token('setup_repair_orphans') }}">
                                                            <div class="row g-3 align-items-end">
                                                                <div class="col-md-8">
                                                                    <label for="targetTenant" class="form-label">Ziel-Mandant</label>
                                                                    <select class="form-select" id="targetTenant" name="target_tenant_id" required>
                                                                        <option value="">Bitte Mandanten auswählen...</option>
                                                                        {% if backup_restore_result.tenants is defined %}
                                                                            {% for tenant in backup_restore_result.tenants %}
                                                                                <option value="{{ tenant.id }}">
                                                                                    {{ tenant.name }}
                                                                                    {% if tenant.identifier is defined and tenant.identifier %}
                                                                                        ({{ tenant.identifier }})
                                                                                    {% endif %}
                                                                                </option>
                                                                            {% endfor %}
                                                                        {% endif %}
                                                                    </select>
                                                                </div>
                                                                <div class="col-md-4">
                                                                    <button type="submit" class="btn btn-warning w-100">
                                                                        <i class="fas fa-wrench me-2"></i>
                                                                        Jetzt reparieren
                                                                    </button>
                                                                </div>
                                                            </div>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        {% endif %}

                                        {# Risk-Asset and Incident-Asset Repair #}
                                        {% if backup_restore_result.risks_without_assets is defined and backup_restore_result.risks_without_assets > 0 %}
                                            <div class="alert alert-info mt-3">
                                                <h6 class="alert-heading">
                                                    <i class="fas fa-link me-2"></i>
                                                    Asset-Zuordnungen reparieren
                                                </h6>
                                                <p>
                                                    <strong>{{ backup_restore_result.risks_without_assets }}</strong> Risiken haben keine Asset-Zuordnung. Sie können diese im
                                                    <a href="{{ path('admin_data_repair_index') }}" class="alert-link">Admin Data Repair Tool</a>
                                                    korrigieren.
                                                </p>
                                            </div>
                                        {% endif %}
                                    {% else %}
                                        <div class="alert alert-danger">
                                            <i class="fas fa-exclamation-circle me-2"></i>
                                            <strong>Fehler bei der Wiederherstellung:</strong> {{ backup_restore_result.message }}
                                        </div>
                                    {% endif %}
                                </div>
                            {% endif %}
                        </div>
                    </div>

                    <hr class="my-4">
                    <h5 class="mb-3">
                        <i class="fas fa-database me-2"></i>
                        Oder: Beispiel-Daten importieren
                    </h5>

                    {% if sample_data|length > 0 %}
                        <form method="post" action="{{ path('setup_step9_sample_data_import') }}">
                            <input type="hidden" name="_token" value="{{ csrf_token('setup_sample_data') }}">
                            <h5 class="mb-3 mt-4">
                                <i class="fas fa-toggle-on me-2"></i>
                                Verfügbare Beispiel-Daten
                            </h5>

                            {% for key, data in sample_data %}
                                <div class="card mb-3">
                                    <div class="card-body">
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" name="samples[{{ key }}]" value="1" id="sample_{{ key }}">
                                            <label class="form-check-label w-100" for="sample_{{ key }}">
                                                <div class="d-flex align-items-start">
                                                    <div class="flex-grow-1">
                                                        <h6 class="mb-1">{{ data.name }}</h6>
                                                        <p class="text-muted mb-2">{{ data.description }}</p>
                                                        {% if data.command is defined %}
                                                            <small class="text-muted">
                                                                <i class="fas fa-terminal me-1"></i>
                                                                Command: <code>{{ data.command }}</code>
                                                            </small>
                                                        {% elseif data.file is defined %}
                                                            <small class="text-muted">
                                                                <i class="fas fa-file me-1"></i>
                                                                File: <code>{{ data.file }}</code>
                                                            </small>
                                                        {% endif %}
                                                        {% if data.required_modules is defined %}
                                                            <div class="mt-2">
                                                                <small class="text-muted">
                                                                    <i class="fas fa-puzzle-piece me-1"></i>
                                                                    Module:
                                                                    {% for module in data.required_modules %}
                                                                        <span class="badge bg-secondary">{{ module }}</span>
                                                                    {% endfor %}
                                                                </small>
                                                            </div>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                            </label>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}

                            <div class="d-flex justify-content-between mt-4">
                                <a href="{{ path('setup_step8_base_data') }}" class="btn btn-outline-secondary">
                                    <i class="fas fa-arrow-left me-2"></i>
                                    Zurück
                                </a>
                                <div>
                                    <button type="button" class="btn btn-outline-secondary me-2" onclick="document.getElementById('skip-form').submit()">
                                        <i class="fas fa-forward me-2"></i>
                                        Überspringen
                                    </button>
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-download me-2"></i>
                                        Ausgewählte Daten importieren
                                    </button>
                                </div>
                            </div>
                        </form>

                        <form id="skip-form" method="post" action="{{ path('setup_step9_sample_data_skip') }}" style="display: none;">
                            <input type="hidden" name="_token" value="{{ csrf_token('setup_sample_skip') }}">
                        </form>
                    {% else %}
                        <div class="alert alert-info" role="alert">
                            <i class="fas fa-info-circle me-2"></i>
                            Keine Beispiel-Daten für die ausgewählten Module verfügbar.
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a href="{{ path('setup_step8_base_data') }}" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>
                                Zurück
                            </a>
                            <a href="{{ path('setup_step10_complete') }}" class="btn btn-primary">
                                Weiter zu Schritt 10: Abschluss
                                <i class="fas fa-arrow-right ms-2"></i>
                            </a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
