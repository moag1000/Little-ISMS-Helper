# =============================================================================
# LITTLE ISMS HELPER - MINIMAL CONFIGURATION
# =============================================================================
# This is the absolute minimum configuration required to start the web setup wizard.
# The wizard will guide you through database and admin user setup interactively.
# =============================================================================

# Environment (do not change during initial setup)
APP_ENV=prod
APP_DEBUG=0

# Secret will be generated automatically on first start if not set
APP_SECRET=

# Temporary SQLite database for setup wizard
# Will be replaced with your actual database configuration during setup
DATABASE_URL=sqlite:///%kernel.project_dir%/var/setup.db

# Mailer (optional - can be configured later in admin panel)
MAILER_DSN=null://null

###> nelmio/cors-bundle ###
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
###< nelmio/cors-bundle ###
